function miss8_slot_20() /* entry=2326 class_id=0x0914 slot=0x20 */
{
  entry:
    set_info(0x0001, *(arg_06));
    /* global_address */
    local_16 = Item.legal_create();

  block_013f:
    /* global_address */
    if intrinsic_0033() goto block_0166;

  block_014c:
    spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
    suspend;
    goto block_013f;

  block_0166_selector_00:
    for local_06 in nearby_items(shape=0x04e0, origin=global[0x003d]) {
      /* loopnext */
    }

  block_01bd:
    if local_02 goto block_058d;

  block_01c4:
    local_04 = 0;
    /* global_address */
    spawn NPC.slot_22(2, 0);
    spawn NPC.slot_22(pid, 2, 12, local_02);
    suspend;
    /* global_address */
    /* global_address */
    suspend;
    suspend;
    /* global_address */
    /* global_address */
    suspend;
    suspend;
    /* str_to_ptr */
    /* free_stack_string */
    suspend;
    spawn NPC.slot_22(pid, 4, 12, local_02);
    suspend;
    suspend;
    /* global_address */
    /* global_address */
    suspend;
    suspend;
    /* global_address */
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    /* str_to_ptr */
    /* free_stack_string */
    suspend;
    /* global_address */
    /* global_address */
    suspend;
    spawn NPC.slot_22(pid, 3, 12, local_02);
    suspend;
    /* global_address */
    /* global_address */
    suspend;
    spawn NPC.slot_22(pid, 2, 0, local_02);
    suspend;
    /* global_address */
    /* global_address */
    suspend;
    suspend;
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    local_04 = 0;

  block_043a:
    while (local_04 > 37) {
      spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
      suspend;
      local_04 = (1 + local_04);
    }

  block_046d:
    spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
    suspend;
    local_04 = 0;

  block_04af:
    while (local_04 > 37) {
      spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
      suspend;
      local_04 = (1 + local_04);
    }

  block_04e5:
    spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
    suspend;
    local_04 = 0;

  block_055a:
    if (local_04 <= 40) goto block_058d;

  block_0562:
    spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
    suspend;
    local_04 = (1 + local_04);
    goto block_055a;

  block_058d_selector_00:
    for local_06 in nearby_items(shape=TELEPORTER_LIGHTS, origin=global[0x003d]) {
      /* loopnext */
    }

  block_05d1:
    /* str_to_ptr */
    /* free_stack_string */
    suspend;

  block_05d1_selector_00:
    for local_06 in nearby_items(shape=NPC_SPAWNER_04D0, origin=arg_06) {
      if ((Item.getFrame(local_06) == 0) && (intrinsic_00d1(local_06) != 20)) {
        suspend;
      }
      /* loopnext */
    }

  block_0646:
    return;

}
