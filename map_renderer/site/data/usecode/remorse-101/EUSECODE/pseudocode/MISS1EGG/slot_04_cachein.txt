function miss1egg_cachein() /* entry=2319 class_id=0x090d slot=0x04 */
{
  set_info(0x0213, *(arg_06));
  process_exclude();
  if (Item.fallProbably_03E(arg_06) == 30) {
    if (!SpriteProcess.createSprite()) {
      spawn FREE.waitNTimerTicks(pid, 1, 0x00000000);
      suspend;
      suspend;
      return;
    }
    else {
      suspend;
      spawn FREE.waitNTimerTicks(pid, 1, 0x00000000);
      suspend;
      spawn FREE.waitNTimerTicks(pid, 1, 0x00000000);
      suspend;
      suspend;
    }
  }
  return;
}
