function rechflew_gotHit() /* entry=348 class_id=0x015a slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  local_02 = arg_10;
  if (((arg_10 == 1) && (Item.getFrame(arg_06) == 0)) && (Actor.getDefaultActivity2(arg_06) == 0)) {
    if (!Item.getSurfaceWeight(*(arg_06), local_02)) {
      return;
    }
    else {
      while (Actor.isBusy(&global[0x003d])) {
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
      }
      suspend;
      for local_02 in nearby_items(shape=0x0158, origin=arg_06) {
      }
      spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
      suspend;
      while (Item.cast(0x0254, global[0x003d])) {
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
      }
      for local_02 in nearby_items(shape=0x0158, origin=arg_06) {
      }
    }
  }
  return;
}
