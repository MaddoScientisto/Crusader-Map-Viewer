function item_slot_2b() /* entry=2592 class_id=0x0a1e slot=0x2b */
{
  set_info(0x0001, *(arg_06));
  spawn FREE.waitNTimerTicks(intrinsic_0018(pid, 0x00c8, 100), 0x00000000);
  suspend;
  local_04 = *(arg_06);
  if (intrinsic_0016(local_04) == 0x0456) {
    set_info(0x00f0, local_02);
    if (!intrinsic_009f()) {
      while ((Item.getFrame(local_02) == 9) && (Item.isOnScreen(arg_06) != 1)) {
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
      }
    }
    return;
  }
  else if (intrinsic_0016(local_04) == 0x044a) {
    set_info(0x00f0, local_02);
    if (!intrinsic_009f()) {
      while ((Item.getFrame(local_02) == 9) && (Item.isOnScreen(arg_06) != 1)) {
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
      }
    }
    return;
  }
  else if (intrinsic_0016(local_04) == 0x0085) {
    set_info(0x00f0, local_02);
    if (!intrinsic_009f()) {
      while ((Item.getFrame(local_02) == 9) && (Item.isOnScreen(arg_06) != 1)) {
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
      }
    }
    return;
  }
  return;
}
