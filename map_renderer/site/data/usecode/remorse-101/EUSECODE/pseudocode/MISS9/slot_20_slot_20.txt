function miss9_slot_20() /* entry=2327 class_id=0x0915 slot=0x20 */
{
  entry:
    set_info(0x0001, *(arg_06));

  block_0133:
    while (!intrinsic_0033()) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }

  block_015a:
    /* global_address */
    /* global_address */
    suspend;

  block_015a_selector_00:
    /* loop_selector local_08 in nearby_items(shape=0x04c6, origin=arg_06) */

  block_0193:
    if condition goto block_019e;

  block_0196:
    local_04 = local_08;
    /* loopnext */
    goto block_0193;

  block_019e_selector_00:
    /* loop_selector local_08 in nearby_items(shape=0x037a, origin=arg_06) */

  block_01b8:
    if condition goto block_01d2;

  block_01bb:
    if (intrinsic_00d1(local_08) != 99) goto block_01ce;

  block_01ca:
    local_06 = local_08;

  block_01ce:
    /* loopnext */
    goto block_01b8;

  block_01d2_selector_00:
    for local_08 in nearby_items(shape=0x053a, origin=arg_06) {
      local_02 = local_08;
      /* loopnext */
    }

  block_01f7:
    if local_02 goto block_0426;

  block_01fe_selector_00:
    /* loop_selector local_08 in nearby_items(shape=15, origin=arg_06) */

  block_0215:
    if condition goto block_0230;

  block_0218:
    if intrinsic_00d1(local_08) goto block_022c;

  block_022c:
    /* loopnext */
    goto block_0215;

  block_0232:
    while ((retval > 2) && (local_10 == 6)) {
      /* global_address */
      /* global_address */
      /* global_address */
      spawn NPC.slot_22(intrinsic_0034(intrinsic_0013(pid, 1, Item.legal_create(Item.legal_create(arg_06), intrinsic_0013(arg_06)))));
      suspend;
      local_10 = (1 + local_10);
    }

  block_029a:
    /* global_address */
    /* global_address */
    suspend;
    spawn FREE.waitNTimerTicks(pid, 120, 0x00000000);
    suspend;
    spawn FREE.slot_3d(pid, Item.legal_create(local_02), intrinsic_0013(local_02), 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 100, 0x00000000);
    suspend;
    /* str_to_ptr */
    /* free_stack_string */
    suspend;
    spawn FREE.waitNTimerTicks(pid, 100, 0x00000000);
    suspend;
    spawn FREE.slot_3e(pid, 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 120, 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 100, 0x00000000);
    suspend;

  block_029a_selector_00:
    for local_08 in nearby_items(shape=TELEPORTER_LIGHTS, origin=arg_06) {
      /* loopnext */
    }

  block_03ff:
    spawn FREE.slot_26(pid, "9f", 0x00000000);
    /* free_stack_string */
    suspend;

  block_0426:
    return;

}
