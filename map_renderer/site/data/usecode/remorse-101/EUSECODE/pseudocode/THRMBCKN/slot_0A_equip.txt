function thrmbckn_equip() /* entry=1384 class_id=0x0566 slot=0x0a */
{
  set_info(0x0211, *(arg_06));
  process_exclude();
  if (arg_10) {
    while (intrinsic_003a(4, arg_06)) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }
    while (intrinsic_003a(4, arg_06)) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }
    while (intrinsic_003a(4, arg_06)) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }
    for local_02 in selector_0x42(arg0=0x0564, arg1=0x0565, arg2=(6 * 32), origin=arg_06) {
      /* loopnext */
    }
    /* loopscr */
    /* loopscr */
    /* loop */
    while (condition) {
      if (intrinsic_000b(local_04) == intrinsic_00e3(arg_06)) {
        local_08 = 3;
        if (intrinsic_0029() > 2) {
          local_06 = 10;
        }
        else {
          local_06 = 5;
        }
        spawn FREE.waitNTimerTicks(pid, 40, 0x00000000);
        suspend;
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
        return;
      }
      /* loopnext */
    }
  }
  return;
}
