function cor_boot_equip() /* entry=866 class_id=0x0360 slot=0x0a */
{
  set_info(0x0211, *(arg_06));
  process_exclude();
  if (Item.getFrame(arg_06) == 10) {
    return;
  }
  FREE.slot_33(0x021b, *(arg_06));
  /* global_address */
  /* global_address */
  while (Item.getFrame(arg_06) == 9) {
    spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
    suspend;
  }
  spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
  suspend;
  if (global[0x0020] == 2) {
    /* str_to_ptr */
    /* free_stack_string */
    suspend;
  }
  else if (global[0x0020] == 8) {
    /* str_to_ptr */
    /* free_stack_string */
    suspend;
  }
  else if (global[0x0020] == 12) {
    /* str_to_ptr */
    /* free_stack_string */
    suspend;
  }
  local_02 = 0;
  while (local_02 > 9) {
    spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
    suspend;
    local_02 = (1 + local_02);
  }
  spawn COR_BOOT.enterFastArea(arg_06);
  return;
}
