function satarg_use() /* entry=1355 class_id=0x0549 slot=0x01 */
{
  entry:
    set_info(0x0207, *(arg_06));
    process_exclude();
    if Item.getFrame(arg_06) goto block_00ee;

  block_00eb:
    goto block_0438;

  block_00f6:
    while (!intrinsic_0033()) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }

  block_011d:
    /* global_address */
    /* global_address */
    suspend;
    spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    /* str_to_ptr */
    /* free_stack_string */
    suspend;
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    spawn ITEM.slot_28(pid, 0, 1, arg_06);
    suspend;
    spawn FREE.slot_3b(0x00000000);

  block_011d_selector_00:
    /* loop_selector local_02 in nearby_items(shape=NUMBERS, origin=arg_06) */

  block_01fd:
    if condition goto block_023f;

  block_0200:
    if (intrinsic_00e3(local_02) != 0) goto block_023b;

  block_020f:
    spawn FREE.waitNTimerTicks(intrinsic_0018(pid, 30, 20), 0x00000000);
    suspend;

  block_023b:
    /* loopnext */
    goto block_01fd;

  block_023f_selector_00:
    /* loop_selector local_02 in nearby_items(shape=0x03c1, origin=arg_06) */

  block_0259:
    if condition goto block_02b8;

  block_025c:
    spawn ITEM.slot_28(pid, 0, 1, local_02);
    suspend;
    if (Item.getFrame(local_02) < 2) goto block_0294;

  block_0294:
    spawn FREE.waitNTimerTicks(intrinsic_0018(pid, 40, 20), 0x00000000);
    suspend;
    /* loopnext */
    goto block_0259;

  block_02b8_selector_00:
    for local_02 in nearby_items(shape=0x03bf, origin=arg_06) {
      spawn ITEM.slot_28(pid, 0, 1, local_02);
      suspend;
      spawn FREE.waitNTimerTicks(intrinsic_0018(pid, 50, 30), 0x00000000);
      suspend;
      /* loopnext */
    }

  block_0332:
    spawn FREE.waitNTimerTicks(pid, 0x00c8, 0x00000000);
    suspend;
    spawn FREE.slot_26(pid, "15d", 0x00000000);
    /* free_stack_string */
    suspend;
    spawn FREE.waitNTimerTicks(pid, 0x00c8, 0x00000000);
    suspend;
    /* pop_global */
    spawn TRIGGER.slot_2a(1, pid, 0, *(arg_06), arg_06);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 0x00c8, 0x00000000);
    suspend;
    if (intrinsic_0029() > 1) goto block_03db;

  block_03c3:
    spawn TRIGGER.slot_2a(pid, 1, *(arg_06), arg_06);
    suspend;

  block_03db:
    spawn FREE.waitNTimerTicks(pid, 0x00c8, 0x00000000);
    suspend;

  block_03db_selector_00:
    /* loop_selector local_02 in nearby_items(shape=TELEPORTER_LIGHTS, origin=arg_06) */

  block_040b:
    if condition goto block_0436;

  block_040e:
    if (intrinsic_00e3(local_02) != 0x00ff) goto block_0432;

  block_0432:
    /* loopnext */
    goto block_040b;

  block_0438:
    return;

}
