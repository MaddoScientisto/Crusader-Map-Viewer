function surcamns_slot_22() /* entry=1224 class_id=0x04c6 slot=0x22 */
{
  entry:
    set_info(0x00f0, 0);

  entry_selector_00:
    for local_02 in selector_0x42(arg0=0x04c8, arg1=0x01cd, arg2=(50 * 32), origin=arg_10) {
      if (!Actor.isNPC(local_02)) {
        local_03 = 1;
      }
      /* loopnext */
    }

  block_0846:
    /* global_address */
    spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
    suspend;
    if local_03 goto block_08bb;

  block_0893:
    while (intrinsic_001d() == 1) {
      spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
      suspend;
    }

  block_08b8:
    goto block_08d3;

  block_08bb:
    spawn FREE.waitNTimerTicks(pid, 0x00c8, 0x00000000);
    suspend;

  block_08d3:
    /* global_address */
    /* global_address */
    /* global_address */
    /* global_address */
    return;

}
