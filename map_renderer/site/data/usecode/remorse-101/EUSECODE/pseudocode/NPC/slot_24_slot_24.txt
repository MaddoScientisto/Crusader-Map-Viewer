function npc_slot_24() /* entry=2579 class_id=0x0a11 slot=0x24 */
{
  set_info(0x020a, *(arg_06));
  if (global[0x003d] == *(arg_06)) {
    /* pop_global */
    while (Actor.isBusy(&global[0x003d])) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }
  }
  for local_02 in selector_0x42(arg0=0x01db, arg1=0x043d, arg2=(5 * 32), origin=arg_06) {
    local_04 = local_02;
  }
  if (local_04) {
    if (Item.getFrame(local_04) == 1) {
      suspend;
      spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
      suspend;
      suspend;
    }
    else {
      suspend;
      spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
      suspend;
      suspend;
    }
    if (AudioProcess.isSFXPlayingForObject(52, &global[0x003d])) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
  }
  return;
}
