function var_boot_equip() /* entry=1118 class_id=0x045c slot=0x0a */
{
  set_info(0x0211, *(arg_06));
  process_exclude();
  if (Item.getFrame(arg_06) == 5) {
    return;
  }
  else {
    FREE.slot_33(0x021b, *(arg_06));
    while (Item.getFrame(arg_06) != 4) {
      spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
      suspend;
    }
    spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
    suspend;
    if (global[0x0020] == 3) {
      /* str_to_ptr */
      /* free_stack_string */
      suspend;
    }
    else if (global[0x0020] == 4) {
      /* str_to_ptr */
      /* free_stack_string */
      suspend;
    }
    else if (global[0x0020] == 6) {
      /* str_to_ptr */
      /* free_stack_string */
      suspend;
    }
    else if (global[0x0020] == 7) {
      /* str_to_ptr */
      /* free_stack_string */
      suspend;
    }
    else if (global[0x0020] == 9) {
      /* str_to_ptr */
      /* free_stack_string */
      suspend;
    }
    local_02 = 0;
    while (local_02 <= 4) {
      spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
      suspend;
      local_02 = (1 + local_02);
    }
    spawn VAR_BOOT.enterFastArea(arg_06);
  }
  return;
}
