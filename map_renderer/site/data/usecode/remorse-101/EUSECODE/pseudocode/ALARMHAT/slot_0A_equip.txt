function alarmhat_equip() /* entry=1379 class_id=0x0561 slot=0x0a */
{
  set_info(0x0211, *(arg_06));
  process_exclude();
  if (Item.getFrame(arg_06)) {
    for local_02 in nearby_items(shape=NPC_SPAWNER_04D0, origin=arg_06) {
      if (Item.getFrame(local_02) == 0) {
        suspend;
      }
      /* loopnext */
    }
    return;
  }
  else if (!Item.isOnScreen(arg_06)) {
    for local_04 in nearby_items(family=6, origin=arg_06) {
      if (Actor.isNPC(local_04)) {
        if ((Item.getZ(local_04) > (Item.getZ(arg_06) - 10)) && (Item.getZ(local_04) < (Item.getZ(arg_06) + 10))) {
          return;
        }
      }
      /* loopnext */
    }
    for local_02 in nearby_items(shape=NPC_SPAWNER_04D0, origin=arg_06) {
      if (Item.getFrame(local_02) == 0) {
        suspend;
      }
      /* loopnext */
    }
  }
  return;
}
