function g_pool_gotHit() /* entry=351 class_id=0x015d slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  local_02 = arg_10;
  FREE.slot_2c(arg_12, arg_10);
  if (retval == 4) {
    spawn ITEM.slot_28(pid, 1, 1, arg_06);
    suspend;
  }
  if ((arg_10 <= 0x0100) && (arg_10 != intrinsic_001d())) {
    return;
  }
  else {
    while (1) {
      /* global_address */
      if (intrinsic_0057(*(arg_06))) {
        FREE.slot_20(8, 20);
        /* global_address */
        /* global_address */
        spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
        suspend;
      }
      else {
        return;
      }
    }
  }
  return;
}
