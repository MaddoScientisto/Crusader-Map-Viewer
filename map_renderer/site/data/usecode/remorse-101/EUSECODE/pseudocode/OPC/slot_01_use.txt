function opc_use() /* entry=958 class_id=0x03bc slot=0x01 */
{
  entry:
    set_info(0x0207, *(arg_06));
    process_exclude();
    ITEM.slot_23(0x1000, arg_06);
    /* global_address */
    if (intrinsic_0011() != 21) goto block_0218;

  block_00fb:
    /* global_address */
    /* global_address */
    suspend;
    /* str_to_ptr */
    /* free_stack_string */
    suspend;
    /* pop_global */
    spawn FREE.waitNTimerTicks(1, pid, 20, 0x00000000);
    suspend;
    spawn TRIGGER.slot_2a(pid, 0, *(arg_06), arg_06);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 0x00b4, 0x00000000);
    suspend;
    /* global_address */
    if (!Kernel.resetRef()) goto block_01c6;

  block_01a8:
    spawn FREE.slot_26(pid, "11g", 0x00000000);
    /* free_stack_string */
    suspend;

  block_01c6_selector_00:
    for local_02 in nearby_items(shape=TELEPORTER_LIGHTS, origin=arg_06) {
      /* loopnext */
    }

  block_0213:
    goto block_029f;

  block_0218:
    /* global_address */
    /* global_address */
    suspend;
    /* str_to_ptr */
    /* free_stack_string */
    suspend;
    /* pop_global */
    spawn FREE.waitNTimerTicks(1, pid, 20, 0x00000000);
    suspend;
    spawn TRIGGER.slot_2a(pid, 0, *(arg_06), arg_06);
    suspend;

  block_029f:
    return;

}
