function energy_use() /* entry=1412 class_id=0x0582 slot=0x01 */
{
  set_info(0x0207, *(arg_06));
  process_exclude();
  set_info(0x00f0, 0);
  /* global_address */
  if (intrinsic_007a() == 0x09c4) {
    local_04 = 0x03e8;
  }
  else {
    /* global_address */
    if (intrinsic_007a() == 0x1388) {
      local_04 = 0x07d0;
    }
    else {
      /* global_address */
      if (intrinsic_007a() == 0x2710) {
        local_04 = 0x0bb8;
      }
      else {
        /* global_address */
      }
    }
  }
  /* global_address */
  /* global_address */
  if (intrinsic_007a(intrinsic_007b())) {
    spawn FREE.slot_21(pid, 0x00b0, 0x00000000);
    suspend;
    return;
  }
  else {
    /* global_address */
    /* global_address */
    if (intrinsic_007a((intrinsic_007b() + local_04))) {
      /* global_address */
      /* global_address */
      spawn FREE.slot_21(pid, 0x010d, 0x00000000);
      suspend;
    }
    else {
      /* global_address */
      /* global_address */
      spawn FREE.slot_21(pid, 0x010d, 0x00000000);
      suspend;
    }
    spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
    suspend;
  }
  return;
}
