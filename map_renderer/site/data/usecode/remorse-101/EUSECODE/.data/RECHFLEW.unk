/* synthesized appendix for RECHFLEW */
/* sparse lines above preserve recovered debugger line numbers where available */

/* ===== slot 0x06 gotHit ===== */
function rechflew_gotHit() /* entry=348 class_id=0x015a slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  local_02 = arg_10;
  if (((arg_10 == 1) && (Item.getFrame(arg_06) == 0)) && (Actor.getDefaultActivity2(arg_06) == 0)) {
    if (!Item.getSurfaceWeight(*(arg_06), local_02)) {
      return;
    }
    else {
      Item.andStatus(1, arg_06);
      Ultima8Engine.setAvatarInStasis(1);
      while (Actor.isBusy(&global[0x003d])) {
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
      }
      Item.setNpcNum(Item.getZ(arg_06), (Item.getY(arg_06) - 16), (Item.getX(arg_06) - 32), &global[0x003d]);
      suspend;
      for local_02 in nearby_items(shape=0x0158, origin=arg_06) {
        if (Item.getFrame(local_02) < 2) {
          Item.isCentreOn((Item.getFrame(local_02) + 2), local_02);
        }
      }
      spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
      suspend;
      Actor.setDead();
      Ultima8Engine.setAvatarInStasis(0);
      while (Item.cast(0x0254, global[0x003d])) {
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
      }
      for local_02 in nearby_items(shape=0x0158, origin=arg_06) {
        if ((Item.getFrame(local_02) == 2) || (Item.getFrame(local_02) == 3)) {
          Item.isCentreOn((Item.getFrame(local_02) - 2), local_02);
        }
      }
    }
  }
  return;
}

/* ===== slot 0x09 release ===== */
function rechflew_release() /* entry=348 class_id=0x015a slot=0x09 */
{
  set_info(0x020d, *(arg_06));
  process_exclude();
  if (Intrinsic056(arg_06) == 0) {
    FREE.slot_33(0x0254, global[0x003d]);
    Item.andStatus(0, arg_06);
  }
  return;
}
