/* synthesized appendix for FLORMINE */
/* sparse lines above preserve recovered debugger line numbers where available */

/* ===== slot 0x06 gotHit ===== */
function flormine_gotHit() /* entry=673 class_id=0x029f slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  if (Item.getFrame(arg_06) > 1) {
    return;
  }
  else {
    FREE.slot_2c(arg_12, arg_10);
    local_02 = retval;
    if (local_02) {
      if (local_02 == 4) {
        return;
      }
      else {
        Item.explode(0, 1, arg_06);
        Actor.setImmortal(arg_06);
        Item.isCentreOn(UCMachine.rndRange(4, 2), arg_06);
      }
      return;
    }
    else {
      local_04 = arg_10;
      if (PaletteFaderProcess.fadeToBlackWithParam(*(arg_06), local_04)) {
        if (((arg_10 < 0x00ff) && (arg_10 != Actor.getLastAnimSet())) && (Item.isOnScreen(arg_06) == 0)) {
          return;
        }
        else {
          Item.explode(0, 1, arg_06);
          Actor.setImmortal(arg_06);
          Item.isCentreOn(UCMachine.rndRange(4, 2), arg_06);
        }
      }
    }
  }
  return;
}
