/* synthesized appendix for LASERI */
/* sparse lines above preserve recovered debugger line numbers where available */

/* ===== slot 0x06 gotHit ===== */
function laseri_gotHit() /* entry=471 class_id=0x01d5 slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  FREE.slot_2c(arg_12, arg_10);
  local_04 = retval;
  local_06 = Egg.getEggXRange(arg_06);
  FREE.slot_2c(arg_12, arg_10);
  if (!retval) {
    return;
  }
  else {
    spawn ITEM.slot_27(pid, 50, arg_12, arg_10, arg_06);
    suspend;
    local_08 = process_result;
    if ((local_04 == 4) && (!local_08)) {
      return;
    }
    else if ((local_04 > 0) && (Item.getFrame(arg_06) == 0)) {
      while (condition) {
        if (Egg.getEggXRange(local_02) == local_06) {
          Item.destroy(local_02);
        }
      }
      spawn ITEM.slot_28(pid, 0, 0, arg_06);
      suspend;
      Item.isCentreOn(1, arg_06);
      if (Item.getMapArray(arg_06) != 0) {
        spawn TRIGGER.slot_2a(pid, 0, *(arg_06), arg_06);
        suspend;
        return;
      }
      else {
        spawn TRIGGER.slot_2a(pid, (0 + 0x0080), *(arg_06), arg_06);
        suspend;
      }
    }
  }
  return;
}
