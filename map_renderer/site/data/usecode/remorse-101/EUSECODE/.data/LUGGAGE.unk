/* synthesized appendix for LUGGAGE */
/* sparse lines above preserve recovered debugger line numbers where available */

/* ===== slot 0x06 gotHit ===== */
function luggage_gotHit() /* entry=145 class_id=0x008f slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  FREE.slot_2c(arg_12, arg_10);
  if (retval) {
    spawn ITEM.slot_27(pid, 100, arg_12, arg_10, arg_06);
    suspend;
    if (!process_result) {
      FREE.slot_20(1);
      if (retval) {
        AudioProcess.playSFXCru(38, arg_06);
      }
      else {
        AudioProcess.playSFXCru(39, arg_06);
      }
      return;
    }
    else {
      spawn ITEM.slot_28(pid, 0, 0, arg_06);
      suspend;
      if (Item.getFrame(arg_06) == 1) {
        Item.legal_create(UCMachine.rndRange(3, 2), 0x059f, local_02);
      }
      else if (Item.getFrame(arg_06) == 2) {
        Item.legal_create(UCMachine.rndRange(5, 4), 0x059f, local_02);
      }
      else if (Item.getFrame(arg_06) == 3) {
        Item.legal_create(UCMachine.rndRange(7, 6), 0x059f, local_02);
      }
      else {
        Item.legal_create(UCMachine.rndRange(1, 0), 0x059f, local_02);
      }
      getTargetNPCNumMaybe(Item.getZ(arg_06), Item.getY(arg_06), Item.getX(arg_06), local_02);
      Container.destroyContents(local_02);
      Item.destroy(arg_06);
    }
  }
  return;
}
