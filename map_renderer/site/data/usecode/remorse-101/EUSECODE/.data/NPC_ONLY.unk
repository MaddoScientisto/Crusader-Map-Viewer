/* synthesized appendix for NPC_ONLY */
/* sparse lines above preserve recovered debugger line numbers where available */

/* ===== slot 0x06 gotHit ===== */
function npc_only_gotHit() /* entry=872 class_id=0x0366 slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  if ((arg_10 > 0x0100) || (arg_10 == Actor.getLastAnimSet())) {
    return;
  }
  else {
    local_02 = arg_10;
    local_04 = Actor.GetNPCDataField0x63_00B(local_02);
    local_06 = void AudioProcess:stopAllSFX(arg_06);
    if (Actor.GetNPCDataField0x63_00B(local_02) == void AudioProcess:stopAllSFX(arg_06)) {
      if (Item.getMapArray(arg_06) != 0) {
        if ((Intrinsic056(arg_06) == 0) || (Item.getNPCNum(*(arg_06), local_02) == 0)) {
          return;
        }
        else if (local_06 == 0x00fe) {
          Actor.setCombatTactic(0x00c8, local_02);
        }
        Item.isCentreOn(1, arg_06);
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
        spawn TRIGGER.slot_2a(pid, 0, *(arg_06), arg_06);
        suspend;
        while (1) {
          if ((Intrinsic056(arg_06) != 0) && (Actor.isNPC(local_02) == 1)) {
            spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
            suspend;
          }
          else {
            Item.isCentreOn(0, arg_06);
            spawn TRIGGER.slot_2a(pid, 1, *(arg_06), arg_06);
            suspend;
            spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
            suspend;
            return;
          }
        }
      }
    }
  }
  return;
}
