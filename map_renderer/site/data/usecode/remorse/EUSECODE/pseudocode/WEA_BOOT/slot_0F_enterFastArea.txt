function wea_boot_enterFastArea() /* entry=1117 class_id=0x045b slot=0x0f */
{
  var
    referent, /* [BP+00h] type=0x69 */
    counter, /* [BP-02h] type=0x69 */
    ran; /* [BP-04h] type=0x69 */

  set_info(0x021b, *(arg_06));
  process_exclude();
  spawn FREE.waitNTimerTicks(pid, 0x07d0, 0x00000000);
  suspend;
  suspend;
  FREE.slot_20(pid, 100);
  spawn FREE.waitNTimerTicks((retval + 60), 0x00000000);
  suspend;
  while (true) {
    suspend;
    FREE.slot_20(pid, 100);
    spawn FREE.waitNTimerTicks((retval + 60), 0x00000000);
    suspend;
    if (!Item.getFrame(arg_06)) {
      counter = 0;
      while (counter <= 4) {
        Item.setFrame(counter, arg_06);
        spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
        suspend;
        counter = (1 + counter);
      }
    }
    FREE.slot_20(100);
    if (retval > 50) {
      FREE.slot_20(2);
      ran = (retval + 1);
      counter = 0;
      while (counter <= ran) {
        Item.setFrame(5, arg_06);
        FREE.slot_20(pid, 10);
        spawn FREE.waitNTimerTicks((retval + 30), 0x00000000);
        suspend;
        Item.setFrame(4, arg_06);
        counter = (1 + counter);
      }
    }
    else {
      counter = 0;
      while (counter <= 4) {
        Item.setFrame((4 - counter), arg_06);
        spawn FREE.waitNTimerTicks(pid, 15, 0x00000000);
        suspend;
        counter = (1 + counter);
      }
    }
  }
  return;
}
