function reb_gar_enterFastArea() /* entry=1262 class_id=0x04ec slot=0x0f */
{
  var
    referent, /* [BP+00h] type=0x69 */
    counter; /* [BP-02h] type=0x69 */

  entry:
    set_info(0x021b, *(arg_06));
    process_exclude();
    Item.setQLo(Item.getFrame(arg_06), arg_06);
    if UCMachine.rndRange(10, 1) <= 5 goto block_0178;

  block_0165:
    Item.setFrame(32, arg_06);
    goto block_02ae;

  while (true) {
    suspend;
    spawn FREE.waitNTimerTicks(UCMachine.rndRange(pid, 0x00dc, 120), 0x00000000);
    suspend;
    counter = 1;
    while (counter <= 7) {
      Item.setFrame((Item.getFrame(arg_06) + 1), arg_06);
      spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
      suspend;
      counter = (1 + counter);
    }
    spawn FREE.waitNTimerTicks(UCMachine.rndRange(pid, 0x00dc, 120), 0x00000000);
    suspend;
    counter = 1;
    while (counter <= 7) {
      Item.setFrame((Item.getFrame(arg_06) - 1), arg_06);
      spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
      suspend;
      counter = (1 + counter);
    }
    Item.setFrame(Item.getQLo(arg_06), arg_06);
  }

  return;


  /* post-return metadata (not executable):
    debug_symbol referent [BP+00h] type=0x69 unk1=0x01 unk3=0x00
    debug_symbol counter [BP-02h] type=0x69 unk1=0x00 unk3=0x00
  */
}
