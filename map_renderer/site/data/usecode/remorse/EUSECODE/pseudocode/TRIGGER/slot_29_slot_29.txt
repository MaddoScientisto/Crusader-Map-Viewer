function trigger_slot_29() /* entry=2586 class_id=0x0a18 slot=0x29 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    ref, /* [BP+10h] type=0x69 */
    qHi, /* [BP+12h] type=0x69 */
    npcNum, /* [BP+14h] type=0x69 */
    mapNum, /* [BP+16h] type=0x69 */
    baseLink, /* [BP+18h] type=0x69 */
    searchtype, /* [BP+20h] type=0x69 */
    scmd, /* [BP-02h] type=0x69 */
    cmd, /* [BP-04h] type=0x69 */
    a, /* [BP-06h] type=0x69 */
    b, /* [BP-08h] type=0x69 */
    c, /* [BP-10h] type=0x69 */
    d, /* [BP-12h] type=0x69 */
    aitem, /* [BP-14h] type=0x24 */
    npc, /* [BP-16h] type=0x24 */
    e, /* [BP-18h] type=0x69 */
    f, /* [BP-20h] type=0x69 */
    g, /* [BP-22h] type=0x69 */
    counter, /* [BP-24h] type=0x69 */
    h, /* [BP-26h] type=0x69 */
    i, /* [BP-28h] type=0x69 */
    k; /* [BP-30h] type=0x69 */

  set_info(0x021b, 0);
  aitem = ref;
  cmd = (mapNum & 3);
  switch (cmd) {
  case 0:
    for npc in nearby_items(shape=searchtype, origin=aitem) {
      if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
        a = (qHi / 16);
        b = (qHi & 15);
        if (a == 0) {
          if (GetNPCDataField0x2_050(npc) != 1) {
            Actor.setActivity(1, npc);
          }
          e = Actor.GetNPCDataField0x4_0DD(npc);
          suspend;
          spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
          suspend;
          f = Actor.GetNPCDataField0x4_0DD(npc);
          Actor.setActivity(f, npc);
        }
        else if (GetNPCDataField0x2_050(npc) != 1) {
          Actor.setActivity(1, npc);
        }
        g = Actor.GetNPCDataField0x4_0DD(npc);
        suspend;
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
        if (Actor.isDead(npc) == 1) {
          return;
        }
        counter = 1;
        while (counter <= a) {
          if (Actor.isDead(npc) == 0) {
            suspend;
          }
          spawn FREE.waitNTimerTicks(pid, 1, 0x00000000);
          suspend;
          counter = (1 + counter);
        }
        h = Actor.GetNPCDataField0x4_0DD(npc);
        i = GetNPCDataField0x2_050(npc);
        Actor.setActivity(Actor.GetNPCDataField0x4_0DD(npc), npc);
      }
    }
    return;
  case 1:
    for npc in nearby_items(shape=searchtype, origin=aitem) {
      if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
        Actor.SetNPCDataField0x63_0E6(qHi, npc);
      }
    }
    return;
  case 2:
    for npc in nearby_items(shape=searchtype, origin=aitem) {
    }
    return;
  case 3:
    scmd = (qHi & 7);
    a = (qHi / 8);
    switch (scmd) {
    case 0:
      for npc in nearby_items(shape=searchtype, origin=aitem) {
        if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
          if (a == 1) {
            Actor.setActivity(9, npc);
          }
          else if (a == 2) {
            Actor.setActivity(10, npc);
          }
          else if (a == 3) {
            Actor.setActivity(11, npc);
          }
          else {
            Actor.setActivity(5, npc);
          }
        }
      }
      break;
    case 1:
      for npc in nearby_items(shape=searchtype, origin=aitem) {
        if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
          Actor.setDefaultActivity0(a, npc);
          spawn FREE.waitNTimerTicks(pid, 1, 0x00000000);
          suspend;
          Actor.setActivity(112, npc);
        }
      }
      break;
    case 2:
      for npc in nearby_items(shape=searchtype, origin=aitem) {
        if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
          Actor.setDefaultActivity1(a, npc);
          spawn FREE.waitNTimerTicks(pid, 1, 0x00000000);
          suspend;
          Actor.setActivity(113, npc);
        }
      }
      break;
    case 3:
      for npc in nearby_items(shape=searchtype, origin=aitem) {
        if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
          Actor.setDefaultActivity2(a, npc);
          spawn FREE.waitNTimerTicks(pid, 1, 0x00000000);
          suspend;
          Actor.setActivity(114, npc);
        }
      }
      break;
    case 4:
      for npc in nearby_items(shape=searchtype, origin=aitem) {
        if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
          k = a;
          Actor.setActivity(k, npc);
        }
      }
      break;
    case 5:
      for npc in nearby_items(shape=searchtype, origin=aitem) {
        if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
          spawn NPC.equip(a, npc);
        }
      }
      break;
    }
    return;
  }
  return;
}
