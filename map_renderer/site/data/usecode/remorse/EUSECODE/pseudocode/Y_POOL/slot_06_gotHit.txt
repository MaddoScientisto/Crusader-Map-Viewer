function y_pool_gotHit() /* entry=352 class_id=0x015e slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  local_02 = arg_10;
  FREE.slot_2c(arg_12, arg_10);
  if (retval == 4) {
    spawn ITEM.slot_28(pid, 1, 1, arg_06);
    suspend;
  }
  if ((arg_10 <= 0x0100) && (arg_10 != intrinsic_001b())) {
    return;
  }
  else {
    while (1) {
      /* global_address */
      if (Item.use(*(arg_06))) {
        FREE.slot_20(8, 10);
        /* global_address */
        /* global_address */
        spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
        suspend;
      }
      else {
        return;
      }
    }
  }
  return;
  /* decompilation stopped at SEARCH_SURFACE */
}
