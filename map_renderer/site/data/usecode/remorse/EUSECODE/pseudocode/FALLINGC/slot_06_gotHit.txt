function fallingc_gotHit() /* entry=97 class_id=0x005f slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  /* loopscr */
  /* loop */
  while (condition) {
    if (intrinsic_0016(local_02) == 95) {
    }
    else if ((intrinsic_003c(local_02) == 6) && (Actor.isNPC(local_02) == 0)) {
      spawn ITEM.slot_28(pid, 1, 0, local_02);
      suspend;
    }
    /* loopnext */
  }
  return;
  /* decompilation stopped at SEARCH_SURFACE */
}
