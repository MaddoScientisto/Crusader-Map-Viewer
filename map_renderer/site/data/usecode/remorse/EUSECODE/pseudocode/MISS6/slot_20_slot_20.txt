function miss6_slot_20() /* entry=2324 class_id=0x0912 slot=0x20 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    snell, /* [BP-02h] type=0x24 */
    n, /* [BP-04h] type=0x24 */
    item, /* [BP-06h] type=0x24 */
    door, /* [BP-08h] type=0x24 */
    doorX; /* [BP-10h] type=0x69 */

  set_info(0x0001, *(arg_06));
  Ultima8Engine.setAvatarInStasis(1);
  while (Actor.isBusy(&global[0x003c])) {
    spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
    suspend;
  }
  for n in nearby_items(shape=0x04dc, origin=global[0x003c]) {
    Actor.create(0, 0x04dc, snell);
    Item.popToCoords(Item.getZ(n), Item.getY(n), Item.getX(n), snell);
    Item.destroy(n);
  }
  if (snell) {
    suspend;
    door = 0;
    for item in nearby_items(shape=door_shape_0095, origin=global[0x003c]) {
      if (Item.getQLo(item) == 19) {
        door = item;
        spawn DOOR.slot_20(pid, door);
        suspend;
        doorX = Item.getX(door);
      }
    }
    if (!door) {
      for item in nearby_items(shape=0x0493, origin=global[0x003c]) {
        door = item;
        spawn DOOR.slot_20(pid, door);
        suspend;
        doorX = Item.getX(door);
      }
    }
    if (Item.getX(&global[0x003c]) > doorX) {
      spawn NPC.slot_22(pid, 1, 12, &global[0x003c]);
      suspend;
    }
    else if (Item.getX(&global[0x003c]) < (doorX - 64)) {
      spawn NPC.slot_22(pid, 1, 4, &global[0x003c]);
      suspend;
    }
    spawn NPC.slot_22(pid, 4, 0, &global[0x003c]);
    suspend;
    spawn NPC.slot_22(pid, 2, 14, &global[0x003c]);
    suspend;
    spawn NPC.slot_22(pid, 2, 4, snell);
    suspend;
    spawn NPC.slot_22(pid, 1, 6, snell);
    suspend;
    suspend;
    suspend;
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    suspend;
    playFlic(pid, 0x0096, 0x00fa, "6g", arg_06);
    suspend;
    playFlic(pid, 0x0096, 0x00fa, "6h", arg_06);
    suspend;
    playFlic(pid, 0x0096, 0x00fa, "6i", arg_06);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    spawn NPC.slot_22(pid, 1, 4, snell);
    suspend;
    spawn NPC.slot_22(pid, 1, 2, snell);
    suspend;
    spawn NPC.slot_22(pid, 1, 4, snell);
    suspend;
    suspend;
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    spawn NPC.slot_24(pid, snell);
    suspend;
    Item.destroy(snell);
    suspend;
    Actor.teleport(0, 0, 0, 0, snell);
  }
  Ultima8Engine.setAvatarInStasis(0);
  Item.destroy(arg_06);
  return;
}
