function free_slot_24() /* entry=2574 class_id=0x0a0c slot=0x24 */
{
  var
    eot; /* [BP+06h] type=0x69 */

  set_info(0x0224, 0);
  if (global[0x0006]) {
    return;
  }
  else {
    suspend;
    spawn FREE.waitNTimerTicks(pid, eot, 0x00000000);
    suspend;
    while (true) {
      suspend;
      spawn FREE.waitNTimerTicks(pid, eot, 0x00000000);
      suspend;
      if (!(global[0x0006])) {
        return;
      }
      else {
        playSFX(51);
        eot = (eot - 1);
        if (eot == 0) {
          Ultima8Engine.setAvatarInStasis(1);
          Actor.setDead(&global[0x003c]);
          PaletteFaderProcess.setPalToAllBlack();
          MusicProcess.musicStop();
          spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
          suspend;
          setSomeMovieGlobal();
          playFlic0A9(pid, 0, 0, "b01", &global[0x003c]);
          suspend;
          clearSomeMovieGlobal();
          Game.playCredits();
          return;
        }
      }
    }
  }
  return;

  /* post-return metadata (not executable):
    debug_symbol eot [BP+06h] type=0x69 unk1=0x00 unk3=0x00
  */
}
