function free_slot_3b() /* entry=2574 class_id=0x0a0c slot=0x3b */
{
  var
    counter, /* [BP-02h] type=0x69 */
    alarm, /* [BP-04h] type=0x69 */
    item, /* [BP-06h] type=0x24 */
    n, /* [BP-08h] type=0x24 */
    ref, /* [BP-10h] type=0x69 */
    notAlarmed; /* [BP-11h] type=0x62 */

  set_info(0x022b, 0);
  ref = 1;
  if (World.getAlertActive()) {
    return;
  }
  else {
    Ultima8Engine.setAlertActive();
    if (Actor.getMap((Actor.getMap((Actor.getMap((Actor.getMap((Actor.getMap((Actor.getMap(&global[0x003c]) == 1), &global[0x003c]) == 5), &global[0x003c]) == 9), &global[0x003c]) == 13), &global[0x003c]) == 23), &global[0x003c]) == 27) {
      alarm = 22;
    }
    else if (Actor.getMap((Actor.getMap((Actor.getMap(&global[0x003c]) == 3), &global[0x003c]) == 15), &global[0x003c]) == 25) {
      alarm = 0x0096;
    }
    else if (Actor.getMap((Actor.getMap((Actor.getMap((Actor.getMap(&global[0x003c]) == 7), &global[0x003c]) == 11), &global[0x003c]) == 17), &global[0x003c]) == 24) {
      alarm = 0x00a0;
    }
    else if (Actor.getMap((Actor.getMap((Actor.getMap(&global[0x003c]) == 10), &global[0x003c]) == 19), &global[0x003c]) == 21) {
      alarm = 0x0096;
    }
    else if (Actor.getMap(&global[0x003c]) == 29) {
      alarm = 0x00c4;
    }
    else {
      alarm = 22;
    }
    counter = 0;
    while (counter <= 2) {
      spawn FREE.slot_21(pid, alarm, 0x00000000);
      suspend;
      counter = (1 + counter);
    }
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    if (UCMachine.rndRange((UCMachine.rndRange((UCMachine.rndRange((UCMachine.rndRange((UCMachine.rndRange(10, 0) == 0), 10, 0) == 1), 10, 0) == 2), 10, 0) == 3), 10, 0) == 4) {
      playSFX(123);
    }
    else {
      spawn FREE.slot_21(pid, 123, 0x00000000);
      suspend;
      spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
      suspend;
      playSFX(125);
    }
    for item in nearby_items(shape=0x0561, origin=ref) {
      if (Item.inFastArea(item)) {
        suspend;
      }
    }
    suspend;
    while (true) {
      suspend;
      if (!World.getAlertActive()) {
        return;
      }
      else {
        ref = 1;
        spawn FREE.waitNTimerTicks(pid, (0x04b0 * (5 - Game.getDifficultyLevel())), 0x00000000);
        suspend;
        counter = 0;
        while (counter <= 2) {
          spawn FREE.slot_21(pid, alarm, 0x00000000);
          suspend;
          counter = (1 + counter);
        }
        if (UCMachine.rndRange(100, 0) > 50) {
          playSFX(0x00d4);
        }
        else {
          playSFX(0x00d0);
        }
        for item in nearby_items(shape=0x0561, origin=ref) {
          if (Item.inFastArea(item)) {
            suspend;
          }
        }
      }
    }
  }
  return;
}
