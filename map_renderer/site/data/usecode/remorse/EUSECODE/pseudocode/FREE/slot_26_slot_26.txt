function free_slot_26() /* entry=2574 class_id=0x0a0c slot=0x26 */
{
  var
    flick, /* [BP+06h] type=0x73 */
    localFlick; /* [BP-02h] type=0x73 */

  /* param_pid_chg */
  set_info(1, global[0x003c]);
  localFlick = flick;
  playSFX(0x0097);
  spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
  suspend;
  Ultima8Engine.setAvatarInStasis(1);
  FREE.slot_41();
  while (Actor.NPCGetField0x59Flag1_0AB(&global[0x003c])) {
    spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
    suspend;
  }
  if (!Actor.isDead(&global[0x003c])) {
    while (Actor.isBusy(&global[0x003c])) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }
    suspend;
    suspend;
  }
  Ultima8Engine.setAvatarInStasis(0);
  return;
}
