function npc_slot_2a() /* entry=2579 class_id=0x0a11 slot=0x2a */
{
  var
    referent, /* [BP+00h] type=0x69 */
    phrase, /* [BP+10h] type=0x69 */
    n; /* [BP-02h] type=0x24 */

  n = *(arg_06);
  set_info(0x0205, 0);
  if ((global[0x0008] == 1) || (Item.inFastArea(n) == 0)) {
    return;
  }
  else if (Item.getStatus(n) & 4) {
    return;
  }
  else {
    ITEM.slot_23(4, n);
    while (AudioProcess.isSFXPlayingForObject(phrase, n)) {
      spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
      suspend;
    }
    spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
    suspend;
  }
  return;
}
