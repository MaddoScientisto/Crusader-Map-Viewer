function spider_slot_20() /* entry=2582 class_id=0x0a14 slot=0x20 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    spiderType, /* [BP+10h] type=0x69 */
    spider, /* [BP-02h] type=0x24 */
    target, /* [BP-04h] type=0x24 */
    dest, /* [BP-09h] type=0x24 */
    count, /* [BP-11h] type=0x69 */
    targetLoop; /* [BP-13h] type=0x69 */

  set_info(1, *(arg_06));
  if (!spiderType) {
    ITEM.slot_23(4, arg_06);
    return;
  }
  else if (Item.getStatus(arg_06) & 0x1000) {
    return;
  }
  else {
    ITEM.slot_23(0x1000, arg_06);
    targetLoop = spiderType;
    while (targetLoop >= 10) {
      targetLoop = (targetLoop - 10);
    }
    suspend;
    while (true) {
      suspend;
      if (getTargetNPCNumMaybe() != 0) {
        target = getTargetNPCNumMaybe();
      }
      else {
        target = global[0x003c];
      }
      while ((Item.getZ(target) > (Item.getZ(arg_06) + 16)) || (Item.getZ(target) < (Item.getZ(arg_06) - 16))) {
        spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
        suspend;
      }
      if ((Item.getStatus(arg_06) & 4) || (Item.getFrame(arg_06) > 0)) {
        ITEM.slot_24(4, arg_06);
        ITEM.slot_24(0x1000, arg_06);
        return;
      }
      count = 0;
      while (count <= 5) {
        spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
        suspend;
        count = (1 + count);
      }
      count = 1;
      while (count <= 9) {
        spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
        suspend;
        count = (1 + count);
      }
      while (Item.getRangeIfVisible(target, arg_06) == 0) {
        spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
        suspend;
      }
      spawn SPIDER.slot_21(pid, targetLoop, target, spider, spider);
      suspend;
      if (spiderType < 99) {
        ITEM.slot_24(0x1000, arg_06);
        return;
      }
    }
  }
  return;
}
