function datalink_use() /* entry=1238 class_id=0x04d4 slot=0x01 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    textFile; /* [BP-02h] type=0x73 */

  set_info(0x0207, *(arg_06));
  process_exclude();
  mouseSomethingOffMaybe05D();
  textFile = "MISSION OBJECTIVES:^*";
  if (Actor.getMap(&global[0x003c]) > 30) {
    return;
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 1), &global[0x003c]) == 2) {
    if (!(global[0x0020])) {
      playFlicsomething(0x0096, 0x00fa, "mva1", arg_06);
      suspend;
    }
    if (!(global[0x002f])) {
      textFile = (textFile + "^)Gain keycard from rebel informant.");
    }
    if (!(global[0x0020])) {
      textFile = (textFile + "^)Locate Thermal Coupler on level four and destroy objective with BlastPac.");
    }
    if (global[0x002f] && global[0x0020]) {
      textFile = (textFile + "^)Locate telepad on fourth level and return to rebel base.");
    }
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 3), &global[0x003c]) == 4) {
    if (!(global[0x0030])) {
      textFile = (textFile + "^)PFC. Andrews to confirm coordinates before teleportation into prison area.");
    }
    if (!(global[0x0021])) {
      textFile = (((textFile + "^)Locate and free rebel soldiers. ") + "^Prisoners rumored to be held for experimentation ") + "in Dr. Hoffman's lab.");
    }
    if (global[0x0030] && global[0x0021]) {
      textFile = (textFile + "^)Locate telepad on prison level and return to rebel base.");
    }
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 5), &global[0x003c]) == 6) {
    if (!(global[0x0022])) {
      playFlicsomething(0x0096, 0x00fa, "mva3a", arg_06);
      suspend;
    }
    if (!(global[0x0031])) {
      textFile = (textFile + "^)PFC. Cardova to confirm coordinates before teleportation into industrial complex.");
    }
    if (!(global[0x0022])) {
      textFile = (textFile + "^)Locate PHIR computer and download latest prototypes' plans.");
    }
    if (global[0x0031] && global[0x0022]) {
      textFile = (textFile + "^)Locate telepad on level 1 and return to rebel base.");
    }
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 7), &global[0x003c]) == 8) {
    if (!(global[0x0023])) {
      playFlicsomething(0x0096, 0x00fa, "mva4", arg_06);
      suspend;
    }
    if (!(global[0x0032])) {
      textFile = (textFile + "^)PFC. Andrews to bring down defense screens before objective can be reached.");
    }
    if (!(global[0x0023])) {
      textFile = (textFile + "^)Locate SORC computer on level 3 and destroy it with a BlastPac.");
    }
    if (global[0x0032] && global[0x0023]) {
      textFile = (textFile + "^)Locate telepad on level 4 and return to rebel base.");
    }
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 9), &global[0x003c]) == 10) {
    if (!(global[0x0024])) {
      playFlicsomething(0x0096, 0x00fa, "mva5a", arg_06);
      suspend;
    }
    if (!(global[0x0033])) {
      textFile = (textFile + "^)Gain keycard from rebel informant.");
    }
    if (!(global[0x0024])) {
      textFile = (textFile + "^)Locate MELF computer in Science Wing, level 5 and download Cypher chip plans.");
    }
    if (global[0x0033] && global[0x0024]) {
      textFile = (textFile + "^)Locate telepad in Science Wing, level 4 and return to rebel base.");
    }
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 11), &global[0x003c]) == 12) {
    if (!(global[0x0025])) {
      playFlicsomething(0x0096, 0x00fa, "mva6", arg_06);
      suspend;
    }
    if (!(global[0x0034])) {
      textFile = (textFile + "^)Sgt. Brooks to confirm teleportation coordinates into government complex.");
    }
    if (!(global[0x0025])) {
      textFile = ((textFile + "^)Locate office of Senator Everett Snell on level 2.") + "Transport the senator back to Central for questioning.");
    }
    if (global[0x0034] && global[0x0025]) {
      textFile = (textFile + "^)Return to rebel base.");
    }
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 13), &global[0x003c]) == 14) {
    if (!(global[0x0026])) {
      playFlicsomething(0x0096, 0x00fa, "mva7", arg_06);
      suspend;
    }
    if (!(global[0x0026])) {
      textFile = ((textFile + "^)Locate EEOD nerve gas containment unit on level 7.") + "Destroy objective with FusionPac.");
    }
    if (global[0x0026]) {
      textFile = (textFile + "^)Locate teleporter on level 7 and return to rebel base.");
    }
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 15), &global[0x003c]) == 16) {
    if (!(global[0x0027])) {
      playFlicsomething(0x0096, 0x00fa, "mva8", arg_06);
      suspend;
    }
    if (!(global[0x0027])) {
      textFile = (textFile + "^)Liberate Professor Willmar from WEC prison.");
    }
    else {
      textFile = (textFile + "^)Locate teleporter on level 3 and return to rebel base.");
    }
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 17), &global[0x003c]) == 18) {
    if (!(global[0x0028])) {
      playFlicsomething(0x0096, 0x00fa, "mva9", arg_06);
      suspend;
    }
    if (!(global[0x0028])) {
      textFile = (textFile + "^)Infiltrate governement complex and spy on congressional meeting.");
    }
    if (global[0x0028]) {
      textFile = (textFile + "^)Return to your original telepad for extraction back to rebel base.");
    }
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 19), &global[0x003c]) == 20) {
    if (!(global[0x0029])) {
      playFlicsomething(0x0096, 0x00fa, "mva10", arg_06);
      suspend;
    }
    if (!(global[0x0029])) {
      textFile = (textFile + "^)Locate plans for new experimental weapon in Dr. Hoffman's lab.");
    }
    if (global[0x0029]) {
      textFile = ((textFile + "^)Destroy as many of the experimental Silencer prototypes as possible.") + "^Locate telepad on level 3 to return to rebel base.");
    }
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 21), &global[0x003c]) == 22) {
    if (!(global[0x002a])) {
      playFlicsomething(0x00fa, "mva11a", arg_06);
      suspend;
    }
    if (!(global[0x0035])) {
      textFile = (textFile + "^)PFC. Cardova to bring down teleport defense screens.");
    }
    if (!(global[0x002a])) {
      textFile = (textFile + "^)Locate OPTS computer and download plans to the Vigilance Platform.");
    }
    if (global[0x002a]) {
      textFile = (textFile + "^)Locate teleporter on level 4 and return to rebel base.");
    }
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 23), &global[0x003c]) == 24) {
    if (!(global[0x002b])) {
      playFlicsomething(0x00fa, "mva12a", arg_06);
      suspend;
    }
    if (!(global[0x002b])) {
      textFile = (textFile + "^)Locate OPC computer and upload new targeting coordinates. ");
    }
    if (global[0x002b]) {
      textFile = (textFile + "^)Locate teleporter on level 3 and return to rebel base.");
    }
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 25), &global[0x003c]) == 26) {
    if (!(global[0x002c])) {
      playFlicsomething(0x0096, 0x00fa, "mva13", arg_06);
      suspend;
    }
    if (!(global[0x002c])) {
      textFile = (textFile + "^)Rescue Col. Ely from WEC prison.");
    }
    else {
      textFile = (textFile + "^)Locate teleporter and return to rebel base.");
    }
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 27), &global[0x003c]) == 28) {
    playFlicsomething(0x0096, 0x00fa, "mva14", arg_06);
    suspend;
    if (!(global[0x002d])) {
      textFile = (textFile + "^)Locate shuttle to Vigilance Platform.^)Await troop transfer.^)Lead hit squad to Vigilance Platform.");
    }
    else {
      textFile = (textFile + "^)Take shuttle to Vigilance Platform.");
    }
  }
  else if (Actor.getMap((Actor.getMap(&global[0x003c]) == 29), &global[0x003c]) == 30) {
    if (!(global[0x0038])) {
      if (!(global[0x0038])) {
        playFlicsomething(0x00fa, "mva15a", arg_06);
        suspend;
      }
      textFile = (textFile + "^)Locate and deactivate SATARG system, then destroy weapon control system.");
    }
    if ((global[0x0038] == 1) && (global[0x002e] == 0)) {
      playFlicsomething(0x00fa, "mva15c", arg_06);
      suspend;
      textFile = (textFile + "^)Locate and destroy Lithium Power Core with BlastPac.");
    }
    if (global[0x002e]) {
      textFile = (textFile + "^)Locate escape pod on level 10 and escape Vigilance Platform.");
    }
  }
  else {
    mouseSomethingResume05F();
    return;
  }
  spawn TEXTFILE.slot_20(pid, textFile, arg_06);
  suspend;
  mouseSomethingResume05F();
  return;
}
