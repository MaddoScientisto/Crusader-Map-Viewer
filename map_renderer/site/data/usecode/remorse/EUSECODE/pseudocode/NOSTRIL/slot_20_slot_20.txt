function nostril_slot_20() /* entry=1088 class_id=0x043e slot=0x20 */
{
  entry:
    set_info(1, *(arg_06));
    if intrinsic_00a6() goto block_0259;

  block_0218:
    while (Item.getFrame(local_04) == 21) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }

  block_0256:
    goto block_027f;

  block_0259:
    spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
    suspend;

  block_027f:
    ITEM.slot_24(0x1000, arg_06);
    return;

  /* decompilation stopped at SEARCH_SURFACE */
}
