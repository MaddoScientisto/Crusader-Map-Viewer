function chest_ew_use() /* entry=1362 class_id=0x0550 slot=0x01 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    item, /* [BP-02h] type=0x24 */
    item2, /* [BP-04h] type=0x24 */
    counter; /* [BP-06h] type=0x69 */

  set_info(0x0207, *(arg_06));
  process_exclude();
  if (Item.getFrame(arg_06) > 1) {
    return;
  }
  else if (Item.getFrame(arg_06) == 1) {
    FREE.slot_2d(Item.getZ(arg_06), Item.legal_create(arg_06), intrinsic_0013(arg_06), 5, 0x054b, *(arg_06));
    if (retval) {
      counter = 5;
      while (counter <= 9) {
        spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
        suspend;
        counter = (1 + counter);
      }
    }
    else {
      return;
    }
  }
  else {
    FREE.slot_2d(Item.getZ(arg_06), Item.legal_create(arg_06), intrinsic_0013(arg_06), 0, 0x054b, *(arg_06));
    if (retval) {
      counter = 0;
      while (counter <= 4) {
        spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
        suspend;
        counter = (1 + counter);
      }
    }
    else {
      return;
    }
  }
  spawn TRIGGER.slot_20(pid, 0, *(arg_06), arg_06);
  suspend;
  for item in nearby_items(shape=0x0476, origin=arg_06) {
    if (intrinsic_002b(arg_06) == intrinsic_002b(item)) {
      FREE.slot_2e(item);
      item2 = retval;
      if ((((intrinsic_003c(item2) == 10) || (intrinsic_003c(item2) == 11)) || (intrinsic_003c(item2) == 12)) || (intrinsic_003c(item2) == 13)) {
        if (!intrinsic_0037(1, item2)) {
          /* global_address */
          if (intrinsic_0011() == 40) {
          }
          else {
            /* global_address */
            /* global_address */
            /* global_address */
          }
        }
      }
      else {
        spawn FREE.slot_31(pid, item2, 0x00000000);
        suspend;
      }
    }
    /* loopnext */
  }
  return;
}
