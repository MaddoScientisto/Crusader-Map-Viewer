function special_slot_20() /* entry=1228 class_id=0x04ca slot=0x20 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    cmd, /* [BP+10h] type=0x69 */
    phase, /* [BP+12h] type=0x69 */
    a, /* [BP-02h] type=0x69 */
    npc, /* [BP-04h] type=0x24 */
    q; /* [BP-06h] type=0x69 */

  set_info(0x021b, *(arg_06));
  switch (cmd) {
  case 3:
    a = Actor.getDir(&global[0x003c]);
    if ((a <= 4) || (a >= 12)) {
      spawn TRIGGER.slot_20(pid, phase, *(arg_06), arg_06);
      suspend;
    }
    return;
  case 4:
    a = Actor.getDir(&global[0x003c]);
    if ((a > 4) && (a < 12)) {
      spawn TRIGGER.slot_20(pid, phase, *(arg_06), arg_06);
      suspend;
    }
    return;
  case 5:
    if (Actor.getInCombat(&global[0x003c]) == 1) {
      spawn TRIGGER.slot_20(pid, phase, *(arg_06), arg_06);
      suspend;
    }
    return;
  case 6:
    if (Actor.getInCombat(&global[0x003c]) == 0) {
      spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
      suspend;
      if (Actor.getInCombat(&global[0x003c]) == 0) {
        spawn TRIGGER.slot_20(pid, phase, *(arg_06), arg_06);
        suspend;
      }
    }
    return;
  case 7:
    a = 0;
    for npc in nearby_items(family=6, origin=global[0x003c]) {
      if (npc < 0x0100) {
        a = (1 + a);
      }
    }
    if (a > 4) {
      spawn TRIGGER.slot_20(pid, phase, *(arg_06), arg_06);
      suspend;
    }
    spawn FREE.waitNTimerTicks(pid, (Item.getQHi(arg_06) * 9), 0x00000000);
    suspend;
    return;
  case 8:
    a = 0;
    for npc in nearby_items(family=6, origin=global[0x003c]) {
      if (npc < 0x0100) {
        a = (1 + a);
      }
    }
    if (a <= 4) {
      spawn TRIGGER.slot_20(pid, phase, *(arg_06), arg_06);
      suspend;
    }
    spawn FREE.waitNTimerTicks(pid, (Item.getQHi(arg_06) * 9), 0x00000000);
    suspend;
    return;
  case 9:
    q = Item.getQLo(arg_06);
    FREE.slot_20(q, 3);
    Item.setQLo(retval, arg_06);
    spawn TRIGGER.slot_20(pid, phase, *(arg_06), arg_06);
    suspend;
    Item.setQLo(q, arg_06);
    return;
  }
  return;
}
