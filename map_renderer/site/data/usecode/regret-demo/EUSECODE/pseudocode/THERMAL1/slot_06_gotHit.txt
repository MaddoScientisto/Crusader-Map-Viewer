function thermal1_gotHit() /* entry=1259 class_id=0x04e9 slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  FREE.slot_35(arg_12, arg_10);
  if (retval) {
    local_04 = intrinsic_012b(arg_06);
    if (Item.getFrame(arg_06) > 0) {
      return;
    }
    else {
      spawn ITEM.slot_29(pid, 120, arg_12, arg_10, arg_06);
      suspend;
      /* dword_to_word */
      if (!process_result) {
        FREE.slot_20(1);
        if (retval) {
        }
        return;
      }
      else {
        spawn ITEM.slot_2a(pid, 0, 2, arg_06);
        suspend;
        /* loopscr */
        /* loopscr */
        /* loop */
        while (condition) {
          spawn ITEM.slot_2a(pid, 0, 1, local_02);
          suspend;
          /* loopnext */
        }
        spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
        suspend;
        spawn FREE.slot_2c(pid, 0x00000000);
        suspend;
      }
    }
  }
  return;
}
