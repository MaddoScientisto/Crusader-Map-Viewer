function vir_slot_20() /* entry=1263 class_id=0x04ed slot=0x20 */
{
  entry:
    set_info(0x0223, 0);
    if Actor.getMana(&global[0x001e]) >= 0x00c8 goto block_017e;

  block_0156:
    spawn FREE.slot_21(pid, 0x01a7, 0x00000000);
    suspend;
    goto block_04a8;

  block_017b:
    goto block_018a;

  block_018a:
    local_02 = Intrinsic006A(0x0223, global[0x001e]);
    if local_02 <= 1 goto block_01a5;

  block_01a2:
    goto block_04a8;

  block_01a5:
    while (true) {
      suspend;
      /* loopscr */
      /* loopscr */
      /* loop */
      while (condition) {
      }
      /* loopscr */
      /* loopscr */
      /* loop */
      while (condition) {
      }
      /* loopscr */
      /* loopscr */
      /* loop */
      while (condition) {
        spawn FREE.slot_3b(pid, 2, 0x00000000);
        suspend;
      }
      spawn FREE.slot_3b(pid, 60, 0x00000000);
      suspend;
      if (Actor.getMana(&global[0x001e]) < 0x00c9) {
        /* loopscr */
        /* loopscr */
        /* loopscr */
        /* loopscr */
        /* loop */
        while (condition) {
        }
        /* pop_global */
      }
      if (!(global[0x0009])) {
        /* loopscr */
        /* loopscr */
        /* loop */
        while (condition) {
          if (Item.getShape(local_04) == 40) {
            spawn FREE.slot_3b(pid, 2, 0x00000000);
            suspend;
          }
        }
        /* loopscr */
        /* loopscr */
        /* loop */
        while (condition) {
          ITEM.slot_26(4, local_04);
          if (retval == 1) {
            return;
          }
        }
        /* loopscr */
        /* loopscr */
        /* loop */
        while (condition) {
          ITEM.slot_26(4, local_04);
          if (retval == 1) {
            return;
          }
        }
        return;
      }
    }

  block_04a8:
    return;

}
