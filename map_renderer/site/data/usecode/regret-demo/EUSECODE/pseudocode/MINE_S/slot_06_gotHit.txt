function mine_s_gotHit() /* entry=1343 class_id=0x053d slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  FREE.slot_35(arg_12, arg_10);
  local_02 = retval;
  if (local_02) {
    if (local_02 == 4) {
      return;
    }
    else {
      Item.explode(0, 1, arg_06);
      Actor.setDead(arg_06);
      Item.push(arg_06);
      Item.setShape(88, arg_06);
      Item.isOn(UCMachine.rndRange(2, 0), arg_06);
      Item.popToCoords((Item.getZ(arg_06) + 1), Item.getY(arg_06), Item.getX(arg_06), arg_06);
    }
    return;
  }
  else {
    local_04 = arg_10;
    if (Item.getQLo(*(arg_06), local_04)) {
      if (((arg_10 < 0x00ff) && (arg_10 != World.getControlledNPCNum())) && (Item.isOnScreen(arg_06) == 0)) {
        return;
      }
      else {
        Item.explode(0, 1, arg_06);
        Actor.setDead(arg_06);
        Item.push(arg_06);
        Item.setShape(88, arg_06);
        Item.isOn(UCMachine.rndRange(2, 0), arg_06);
        Item.popToCoords((Item.getZ(arg_06) + 1), Item.getY(arg_06), Item.getX(arg_06), arg_06);
      }
    }
  }
  return;
}
