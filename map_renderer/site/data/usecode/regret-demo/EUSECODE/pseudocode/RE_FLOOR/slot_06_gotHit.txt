function re_floor_gotHit() /* entry=314 class_id=0x0138 slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  local_02 = arg_10;
  if (((arg_10 == 1) && (Item.getFrame(arg_06) < 2)) && (IntrinsicReturn0(arg_06) == 0)) {
    if (!Intrinsic0061(*(arg_06), local_02)) {
      return;
    }
    else {
      Item.setQLo(1, arg_06);
      Item.getPoint(1);
      suspend;
      suspend;
      suspend;
      while (condition) {
        if (!Item.getFrame(local_02)) {
          Item.isOn(1, local_02);
        }
      }
      spawn FREE.slot_3b(pid, 60, 0x00000000);
      suspend;
      Actor.isDead();
      Item.getPoint(0);
      while (Intrinsic006A(0x0254, global[0x001e])) {
        spawn FREE.slot_3b(pid, 2, 0x00000000);
        suspend;
      }
      while (condition) {
        if (Item.getFrame(local_02) == 1) {
          Item.isOn(0, local_02);
        }
      }
    }
  }
  return;
}
