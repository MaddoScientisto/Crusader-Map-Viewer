function shocker_slot_21() /* entry=2583 class_id=0x0a15 slot=0x21 */
{
  entry:
    set_info(0x0205, *(arg_06));
    set_info(0x020c, *(arg_06));

  while (true) {
    suspend;
    spawn FREE.slot_3b(pid, 5, 0x00000000);
    suspend;
    for local_02 in nearby_items(family=6, origin=arg_06) {
      if (Actor.isNPC(local_02) == 1) {
        if (!Actor.setActivity(local_02)) {
          if ((Intrinsic00AD(local_02, arg_06) == 1) && (Item.getFrame(arg_06) < 6)) {
            FREE.slot_20(pid, 8);
            spawn NPC.slot_2f(retval, *(arg_06), local_02);
            suspend;
            spawn FREE.slot_3b(pid, 2, 0x00000000);
            suspend;
            return;
          }
        }
      }
    }
  }

  return;

}
