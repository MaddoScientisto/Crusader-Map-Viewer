function trigpad_gotHit() /* entry=1231 class_id=0x04cd slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  local_02 = arg_10;
  FREE.slot_34(arg_12, arg_10);
  if (retval) {
    return;
  }
  else {
    local_04 = arg_10;
    if (Item.getMapArray(arg_06) != 0) {
      if ((Actor.isBusy(arg_06) == 0) || (Item.isCompletelyOn(*(arg_06), local_04) == 0)) {
        return;
      }
      else {
        spawn FREE.slot_3b(pid, 1, 0x00000000);
        suspend;
        spawn TRIGGER.slot_20(pid, 0, *(arg_06), arg_06);
        suspend;
        while (1) {
          if (Actor.isBusy(arg_06) != 0) {
            spawn FREE.slot_3b(pid, 2, 0x00000000);
            suspend;
          }
          else {
            spawn TRIGGER.slot_20(pid, 1, *(arg_06), arg_06);
            suspend;
            spawn FREE.slot_3b(pid, 2, 0x00000000);
            suspend;
            /* loopscr */
            /* loopscr */
            /* loopscr */
            /* loopscr */
            /* loop */
            while (condition) {
              while (Item.getStatus(local_06) & 0x1000) {
                spawn FREE.slot_3b(pid, 2, 0x00000000);
                suspend;
              }
              spawn FREE.slot_3b(pid, 120, 0x00000000);
              suspend;
              if (Item.getZ(&global[0x001e]) < (Item.getZ(local_06) - 5)) {
                ELEVAT.slot_21(Item.getZ(local_06), local_06);
                if (retval) {
                  spawn ELEVAT.slot_20(local_06);
                }
              }
            }
            return;
          }
        }
      }
    }
  }
  return;
}
