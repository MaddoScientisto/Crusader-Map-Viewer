function luggage_gotHit() /* entry=145 class_id=0x008f slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  FREE.slot_35(arg_12, arg_10);
  if (retval) {
    spawn ITEM.slot_29(pid, 100, arg_12, arg_10, arg_06);
    suspend;
    if (!process_result) {
      FREE.slot_20(1);
      if (retval) {
        Item.playSfxCru(38, arg_06);
      }
      else {
        Item.playSfxCru(39, arg_06);
      }
      return;
    }
    else {
      spawn ITEM.slot_2a(pid, 0, 0, arg_06);
      suspend;
      if (Item.getFrame(arg_06) == 1) {
        Item.create(UCMachine.rndRange(3, 2), 0x059f, local_02);
      }
      else if (Item.getFrame(arg_06) == 2) {
        Item.create(UCMachine.rndRange(5, 4), 0x059f, local_02);
      }
      else if (Item.getFrame(arg_06) == 3) {
        Item.create(UCMachine.rndRange(7, 6), 0x059f, local_02);
      }
      else {
        Item.create(UCMachine.rndRange(1, 0), 0x059f, local_02);
      }
      Item.popToCoords(Item.getZ(arg_06), Item.getY(arg_06), Item.getX(arg_06), local_02);
      Item.destroy(arg_06);
    }
  }
  return;
}
