function dicor1_gotHit() /* entry=783 class_id=0x030d slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  FREE.slot_35(arg_12, arg_10);
  if (!retval) {
    return;
  }
  else {
    local_02 = Item.getFrame(arg_06);
    spawn ITEM.slot_2a(pid, 0, 0, arg_06);
    suspend;
    if (!(global[0x0005])) {
      suspend;
    }
    for local_06 in nearby_items(family=6, origin=arg_06) {
      NPC.slot_2a(local_06);
      if (retval) {
        spawn NPC.slot_2c(local_06);
      }
      else if (!intrinsic_007b(local_06)) {
        spawn NPC.slot_2d(3, local_06);
      }
      /* loopnext */
    }
  }
  return;
}
