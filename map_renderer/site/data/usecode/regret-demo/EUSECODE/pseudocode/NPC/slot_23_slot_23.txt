function npc_slot_23() /* entry=2580 class_id=0x0a12 slot=0x23 */
{
  set_info(0x020a, *(arg_06));
  if (*(arg_06) == 1) {
    Item.getPoint(1);
    Intrinsic0094(&global[0x001e]);
  }
  for local_02 in nearby_items(shape=DISRUPTER, origin=arg_06) {
    if (Item.getFrame(local_02) == 1) {
      local_09 = 1;
      local_06 = local_02;
    }
  }
  for local_02 in nearby_items(shape=TELEPORTER_LIGHTS, origin=arg_06) {
    local_04 = local_02;
  }
  while (condition) {
    if (Item.getShape(local_02) == 0x018e) {
      if (!Item.getFrame(local_02)) {
        local_08 = 1;
      }
    }
    else if (Item.getFrame(local_02)) {
      local_08 = 1;
    }
  }
  Item.isOn(8, arg_06);
  spawn FREE.slot_3b(pid, 60, 0x00000000);
  suspend;
  Item.playSfxCru(48, &global[0x001e]);
  SpriteProcess.createSprite(Item.getZ(arg_06), Item.getY(arg_06), Item.getX(arg_06), 1, 37, 0, 0x0317);
  if (!local_08) {
    suspend;
  }
  else {
    suspend;
  }
  if (*(arg_06) == 1) {
    Item.unequip();
    MusicProcess.unpauseMusic();
    Actor.setImmortal(&global[0x001e]);
    Item.getPoint(0);
  }
  if (local_09) {
    SpriteProcess.createSprite(Item.getZ(local_06), Item.getY(local_06), Item.getX(local_06), 5, 9, 0, 0x04f1);
    Item.playSfxCru(13, local_06);
    spawn FREE.slot_3b(pid, 20, 0x00000000);
    suspend;
    Item.playSfxCru(13, local_06);
    spawn FREE.slot_3b(pid, 20, 0x00000000);
    suspend;
    Item.playSfxCru(13, local_06);
    spawn FREE.slot_3b(pid, 20, 0x00000000);
    suspend;
    while (!Actor.setCombatTactic(arg_06)) {
      MusicProcess.playMusic(20, Actor.getHp(arg_06), 8, 0, arg_06);
      spawn FREE.slot_3b(pid, 2, 0x00000000);
      suspend;
    }
  }
  return;
}
