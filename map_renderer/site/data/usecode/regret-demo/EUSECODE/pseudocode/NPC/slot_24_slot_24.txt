function npc_slot_24() /* entry=2580 class_id=0x0a12 slot=0x24 */
{
  set_info(0x020a, *(arg_06));
  if (global[0x001e] == *(arg_06)) {
    setUnkFlagA4();
    Intrinsic0094(&global[0x001e]);
    Item.getPoint(1);
    Game.isViolenceEnabled();
    while (Item.legal_move(&global[0x001e])) {
      spawn FREE.slot_3b(pid, 2, 0x00000000);
      suspend;
    }
    if (Item.fall(&global[0x001e])) {
      suspend;
    }
  }
  while (condition) {
    local_04 = local_02;
  }
  if (local_04) {
    Item.getCY(AudioProcess.stopSFXCru(arg_06), Item.getZ(local_04), (Item.getY(local_04) - 16), (Item.getX(local_04) - 16), arg_06);
    if (Item.getFrame(local_04) == 1) {
      suspend;
      suspend;
      spawn FREE.slot_3b(pid, 60, 0x00000000);
      suspend;
      SpriteProcess.createSprite(Item.getZ(arg_06), Item.getY(arg_06), Item.getX(arg_06), 1, 37, 0, 0x0317);
      Item.playSfxCru(52, arg_06);
      suspend;
    }
    else {
      suspend;
      suspend;
      spawn FREE.slot_3b(pid, 60, 0x00000000);
      suspend;
      SpriteProcess.createSprite(Item.getZ(arg_06), Item.getY(arg_06), Item.getX(arg_06), 1, 37, 0, 0x0317);
      Item.playSfxCru(52, arg_06);
      suspend;
    }
    if (*(arg_06) != World.getControlledNPCNum()) {
      Item.destroy(arg_06);
    }
    Item.isOn(8, arg_06);
    if (Intrinsic0035(52, &global[0x001e])) {
      spawn FREE.slot_3b(pid, 2, 0x00000000);
      suspend;
    }
    spawn FREE.slot_3b(pid, 60, 0x00000000);
    suspend;
  }
  return;
}
