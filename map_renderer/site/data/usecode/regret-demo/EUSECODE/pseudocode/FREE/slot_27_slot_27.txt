function free_slot_27() /* entry=2575 class_id=0x0a0d slot=0x27 */
{
  set_info(0x0229, 0);
  if (Actor.getMana(&global[0x001e]) < 100) {
    return;
  }
  else if (global[0x000d]) {
    /* pop_global */
  }
  else if (Intrinsic006A(0x0229, 0) > 1) {
    return;
  }
  /* pop_global */
  /* loopscr */
  /* loopscr */
  /* loopscr */
  /* loopscr */
  /* loop */
  while (condition) {
  }
  suspend;
  while (true) {
    suspend;
    if (Intrinsic006A(0x0229, 0) > 1) {
      return;
    }
    local_04 = Actor.getMana(&global[0x001e]);
    if (((Ultima8Engine.getUnkCrusaderFlag(&global[0x001e]) == 1) || (global[0x000d] == 0)) || (local_04 < 100)) {
      /* loopscr */
      /* loopscr */
      /* loopscr */
      /* loopscr */
      /* loop */
      while (condition) {
      }
      /* pop_global */
      return;
    }
    if (local_04 > 75) {
    }
    else {
      /* pop_global */
    }
    spawn FREE.slot_3b(pid, 60, 0x00000000);
    suspend;
  }
  return;
}
