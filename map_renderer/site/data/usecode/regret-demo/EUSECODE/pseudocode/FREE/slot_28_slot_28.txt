function free_slot_28() /* entry=2575 class_id=0x0a0d slot=0x28 */
{
  set_info(0x0223, 0);
  if (global[0x0002]) {
    return;
  }
  else {
    /* pop_global */
    suspend;
    while (true) {
      suspend;
      if (!(global[0x0002])) {
        return;
      }
      /* global_address */
      if (!intrinsic_007b()) {
        /* global_address */
        /* pop_global */
        spawn FREE.slot_3b(0, pid, 10, 0x00000000);
        suspend;
        /* str_to_ptr */
        /* free_stack_string */
        /* str_to_ptr */
        /* free_stack_string */
        suspend;
        return;
      }
    }
  }
  return;
}
