function free_slot_25() /* entry=2575 class_id=0x0a0d slot=0x25 */
{
  set_info(0x0215, 0);
  /* pop_global */
  if (global[0x000b] > 1) {
    return;
  }
  else {
    suspend;
    while (true) {
      suspend;
      if (intrinsic_006a(0x0215, 0) > 1) {
        return;
      }
      /* global_address */
      if ((intrinsic_007b() == 1) || (global[0x000b] == 0)) {
        return;
      }
      if (global[0x000d]) {
        /* global_address */
        /* global_address */
        /* global_address */
        /* global_address */
        spawn FREE.slot_3b(pid, 120, 0x00000000);
        suspend;
      }
      else {
        /* global_address */
        /* global_address */
        /* global_address */
        /* global_address */
        /* global_address */
        spawn FREE.slot_3b(pid, 100, 0x00000000);
        suspend;
      }
    }
  }
  return;
}
