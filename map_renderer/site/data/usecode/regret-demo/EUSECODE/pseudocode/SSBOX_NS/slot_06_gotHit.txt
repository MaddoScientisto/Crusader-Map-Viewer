function ssbox_ns_gotHit() /* entry=716 class_id=0x02ca slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  FREE.slot_35(arg_12, arg_10);
  if (retval) {
    if (!Item.getFrame(arg_06)) {
      spawn ITEM.slot_2a(pid, 0, 0, arg_06);
      suspend;
      Item.isOn(UCMachine.rndRange(2, 1), arg_06);
      return;
    }
    else {
      spawn ITEM.slot_29(pid, 100, arg_12, arg_10, arg_06);
      suspend;
      if (!process_result) {
        FREE.slot_20(1);
        if (retval) {
          Item.playSfxCru(38, arg_06);
        }
        else {
          Item.playSfxCru(39, arg_06);
        }
        return;
      }
      else {
        spawn ITEM.slot_2c(pid, arg_06);
        suspend;
        spawn ITEM.slot_2a(pid, 1, 1, arg_06);
        suspend;
      }
    }
  }
  return;
}
