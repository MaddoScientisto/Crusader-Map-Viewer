function spider_slot_22() /* entry=2585 class_id=0x0a17 slot=0x22 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    destX, /* [BP+10h] type=0x69 */
    destY, /* [BP+12h] type=0x69 */
    destZ, /* [BP+14h] type=0x62 */
    vel, /* [BP+16h] type=0x69 */
    currentP, /* [BP-05h] type=0x24 */
    x, /* [BP-07h] type=0x69 */
    y, /* [BP-09h] type=0x69 */
    z, /* [BP-10h] type=0x62 */
    w, /* [BP-12h] type=0x69 */
    h, /* [BP-14h] type=0x69 */
    counter, /* [BP-16h] type=0x69 */
    minor, /* [BP-18h] type=0x69 */
    r, /* [BP-20h] type=0x69 */
    q, /* [BP-22h] type=0x69 */
    oabs, /* [BP-24h] type=0x69 */
    v, /* [BP-26h] type=0x69 */
    ovel, /* [BP-28h] type=0x69 */
    c, /* [BP-30h] type=0x69 */
    axis, /* [BP-32h] type=0x69 */
    ztarg, /* [BP-34h] type=0x69 */
    zcur, /* [BP-36h] type=0x69 */
    dif; /* [BP-38h] type=0x69 */

  set_info(0x00f0, *(arg_06));
  x = Item.getX(arg_06);
  y = Item.getY(arg_06);
  z = Item.getZ(arg_06);
  w = ((destX - x) + 1);
  h = ((destY - y) + 1);
  counter = 0;
  minor = 1;
  ovel = vel;
  c = 0;
  axis = 1;
  FREE.slot_3f(w);
  FREE.slot_3f(retval, h);
  if (retval) {
    axis = 0;
  }
  if (axis == 0) {
    FREE.slot_3f(((h / w) * w));
    q = retval;
    /* mod */
    FREE.slot_3f(h, w);
    r = retval;
    if (w < 0) {
      ovel = (-1 * vel);
    }
    if (h < 0) {
      minor = -1;
    }
    FREE.slot_3f(w);
    oabs = retval;
  }
  else {
    FREE.slot_3f(((w / h) * h));
    q = retval;
    /* mod */
    FREE.slot_3f(w, h);
    r = retval;
    if (h < 0) {
      ovel = (-1 * vel);
    }
    if (w < 0) {
      minor = -1;
    }
    FREE.slot_3f(h);
    oabs = retval;
  }
  suspend;
  spawn FREE.slot_32(pid, 2, 0x00000000);
  suspend;
  zcur = (z & 0x00ff);
  ztarg = (destZ & 0x00ff);
  while (true) {
    suspend;
    spawn FREE.slot_32(pid, 2, 0x00000000);
    suspend;
    zcur = (z & 0x00ff);
    ztarg = (destZ & 0x00ff);
    if (zcur != ztarg) {
      FREE.slot_3f((ztarg - zcur));
      dif = ((retval / 20) + 1);
      if (dif == 0) {
        dif = 1;
      }
      if (zcur < ztarg) {
        z = (z + dif);
      }
      else {
        zcur = (zcur - dif);
        z = (zcur & 0x00ff);
      }
    }
    if (c >= oabs) {
      if (zcur > (ztarg - 2)) {
        if (zcur < (ztarg + 2)) {
          /* word_to_dword */
          /* pop_result */
          return;
        }
      }
      currentP = x;
      local_03 = y;
      local_01 = z;
      if (!Item.andStatus(0, 1, currentP, arg_06)) {
        /* word_to_dword */
        /* pop_result */
        return;
      }
    }
    else {
      c = (c + vel);
      if (axis == 0) {
        currentP = x;
        local_03 = y;
        local_01 = z;
        if (!Item.andStatus(0, 1, currentP, arg_06)) {
          /* word_to_dword */
          /* pop_result */
          return;
        }
        x = (x + ovel);
        v = 1;
        while (v <= vel) {
          counter = (counter + (r + q));
          if (counter >= oabs) {
            counter = (counter - oabs);
            y = (y + minor);
          }
          v = (1 + v);
        }
      }
      else {
        currentP = x;
        local_03 = y;
        local_01 = z;
        if (!Item.andStatus(0, 1, currentP, arg_06)) {
          /* word_to_dword */
          /* pop_result */
          return;
        }
        y = (y + ovel);
        v = 1;
        while (v <= vel) {
          counter = (counter + (r + q));
          if (counter >= oabs) {
            counter = (counter - oabs);
            x = (x + minor);
          }
          v = (1 + v);
        }
      }
    }
  }
  return;
}
