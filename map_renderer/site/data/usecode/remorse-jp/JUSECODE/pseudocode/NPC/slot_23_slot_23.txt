function npc_slot_23() /* entry=2581 class_id=0x0a13 slot=0x23 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    item, /* [BP-02h] type=0x24 */
    pad, /* [BP-04h] type=0x24 */
    padDir; /* [BP-06h] type=0x69 */

  set_info(0x020a, *(arg_06));
  /* global_address */
  if (intrinsic_0011((*(arg_06) != 1)) == 40) {
    /* global_address */
  }
  for item in selector_0x42(arg0=0x01db, arg1=0x043d, arg2=(5 * 32), origin=arg_06) {
    pad = item;
    /* loopnext */
  }
  /* loopscr */
  /* loopscr */
  /* loop */
  while (condition) {
    if (intrinsic_0016(item) == 0x018e) {
      if (!Item.getFrame(item)) {
        padDir = 1;
      }
    }
    else if (Item.getFrame(item)) {
      padDir = 1;
    }
    /* loopnext */
  }
  spawn FREE.slot_32(pid, 60, 0x00000000);
  suspend;
  /* global_address */
  if (!padDir) {
    suspend;
  }
  else {
    suspend;
  }
  /* global_address */
  if (intrinsic_003d(48)) {
    spawn FREE.slot_32(pid, 2, 0x00000000);
    suspend;
  }
  if (*(arg_06) == 1) {
    /* global_address */
  }
  return;
}
