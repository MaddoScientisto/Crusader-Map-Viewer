function and_boot_equip() /* entry=790 class_id=0x0314 slot=0x0a */
{
  var
    referent, /* [BP+00h] type=0x69 */
    event, /* [BP+10h] type=0x69 */
    counter; /* [BP-02h] type=0x69 */

  set_info(0x0211, *(arg_06));
  process_exclude();
  if (Item.getFrame(arg_06) == 12) {
    return;
  }
  else {
    FREE.slot_33(0x021b, *(arg_06));
    while (Item.getFrame(arg_06) != 11) {
      spawn FREE.slot_32(pid, 5, 0x00000000);
      suspend;
    }
    spawn FREE.slot_32(pid, 5, 0x00000000);
    suspend;
    if (global[0x001f] == 2) {
      /* str_to_ptr */
      /* free_stack_string */
      suspend;
    }
    else if (global[0x001f] == 3) {
      /* str_to_ptr */
      /* free_stack_string */
      suspend;
    }
    else if (global[0x001f] == 4) {
      /* str_to_ptr */
      /* free_stack_string */
      suspend;
    }
    else {
      /* str_to_ptr */
      /* free_stack_string */
      suspend;
    }
    counter = 0;
    while (counter <= 11) {
      spawn FREE.slot_32(pid, 10, 0x00000000);
      suspend;
      counter = (1 + counter);
    }
    spawn AND_BOOT.enterFastArea(arg_06);
  }
  return;
}
