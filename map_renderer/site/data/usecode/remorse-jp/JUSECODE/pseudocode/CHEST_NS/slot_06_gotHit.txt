function chest_ns_gotHit() /* entry=1361 class_id=0x054f slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  FREE.slot_2c(arg_12, arg_10);
  if (!retval) {
    return;
  }
  else {
    local_02 = Item.getFrame(arg_06);
    if (local_02 < 2) {
      spawn ITEM.slot_27(pid, 40, arg_12, arg_10, arg_06);
      suspend;
      /* dword_to_word */
      if (!process_result) {
        FREE.slot_20(1);
        if (retval) {
        }
        return;
      }
      else {
        spawn ITEM.slot_28(pid, 0, 0, arg_06);
        suspend;
        ITEM.slot_23(0x1000, arg_06);
      }
    }
  }
  return;
  /* decompilation stopped at SEARCH_SURFACE */
}
