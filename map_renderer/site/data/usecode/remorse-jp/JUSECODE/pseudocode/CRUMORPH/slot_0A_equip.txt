function crumorph_equip() /* entry=794 class_id=0x0318 slot=0x0a */
{
  var
    referent, /* [BP+00h] type=0x69 */
    var, /* [BP+10h] type=0x69 */
    n, /* [BP-02h] type=0x24 */
    lastAct; /* [BP-04h] type=0x69 */

  set_info(0x0211, *(arg_06));
  process_exclude();
  for n in nearby_items(family=6, origin=arg_06) {
    if (Actor.isNPC(n)) {
      if (Actor.GetNPCDataField0x63_00B(n) == legalMoveToPoint(arg_06)) {
        if (Item.getItemFamily(n) == 0) {
          NPC.slot_2b(n);
          if (!retval) {
            return;
          }
          lastAct = Actor.setDefaultActivity1(n);
          Actor.setDefaultActivity2(n);
          Actor.setActivity(1, n);
          Intrinsic056(1, n);
          Item.getSurfaceWeight(1, n);
          Item.isCentreOn(112, n);
          Actor.setDefaultActivity2(n);
          spawn FREE.slot_32(pid, 3, 0x00000000);
          suspend;
          spawn NPC.slot_28(pid, n);
          suspend;
          Item.setFrame(n);
          while (getTargetNPCNumMaybe() > 1) {
            spawn FREE.slot_32(pid, 2, 0x00000000);
            suspend;
          }
          if (Item.getItemFamily(n) == 0) {
            spawn TRIGGER.slot_20(pid, 0, *(arg_06), arg_06);
            suspend;
          }
          else {
            spawn TRIGGER.slot_20(pid, 1, *(arg_06), arg_06);
            suspend;
          }
          return;
        }
      }
    }
  }
  return;
}
