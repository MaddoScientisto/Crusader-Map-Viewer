function cruztrig_gotHit() /* entry=871 class_id=0x0365 slot=0x06 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    ref, /* [BP+10h] type=0x69 */
    vel, /* [BP+12h] type=0x69 */
    item, /* [BP-02h] type=0x24 */
    elev; /* [BP-04h] type=0x24 */

  set_info(0x020c, *(arg_06));
  process_exclude();
  if (ref != getTargetNPCNumMaybe()) {
    return;
  }
  else if (Item.getMapArray(arg_06) != 0) {
    item = ref;
    if ((Actor.getLastAnimSet(arg_06) == 0) || (Item.legal_create(*(arg_06), item) == 0)) {
      return;
    }
    else {
      spawn FREE.slot_32(pid, 2, 0x00000000);
      suspend;
      spawn TRIGGER.slot_20(pid, 0, *(arg_06), arg_06);
      suspend;
      while (1) {
        if (Actor.getLastAnimSet(arg_06) != 0) {
          spawn FREE.slot_32(pid, 2, 0x00000000);
          suspend;
        }
        else {
          spawn TRIGGER.slot_20(pid, 1, *(arg_06), arg_06);
          suspend;
          spawn FREE.slot_32(pid, 2, 0x00000000);
          suspend;
          /* loopscr */
          /* loopscr */
          /* loopscr */
          /* loopscr */
          /* loop */
          while (condition) {
            while (Item.getStatus(elev) & 0x1000) {
              spawn FREE.slot_32(pid, 2, 0x00000000);
              suspend;
            }
            spawn FREE.slot_32(pid, 120, 0x00000000);
            suspend;
            if (Item.getZ(&global[0x003c]) < (Item.getZ(elev) - 5)) {
              ELEVAT.slot_21(Item.getZ(elev), elev);
              if (retval) {
                spawn ELEVAT.slot_20(elev);
              }
            }
          }
          return;
        }
      }
    }
  }
  return;
}
