function fight_ns_gotHit() /* entry=385 class_id=0x017f slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  if (intrinsic_0004(arg_06) & 0x1000) {
    return;
  }
  else {
    FREE.slot_2c(arg_12, arg_10);
    if (!retval) {
      return;
    }
    else {
      spawn ITEM.slot_27(pid, 0x00c8, arg_12, arg_10, arg_06);
      suspend;
      /* dword_to_word */
      if (!process_result) {
        FREE.slot_20(1);
        if (retval) {
        }
        return;
      }
      else {
        /* loopscr */
        /* loopscr */
        /* loop */
        while (condition) {
          ITEM.slot_23(0x1000, local_02);
          if (!Item.getFrame(local_02)) {
            if ((intrinsic_0016(local_02) == 0x017f) || (intrinsic_0016(local_02) == 0x0141)) {
              spawn ITEM.slot_28(pid, 0, 2, local_02);
              suspend;
            }
            else {
              spawn ITEM.slot_28(pid, 0, 1, local_02);
              suspend;
            }
          }
          /* loopnext */
        }
      }
    }
  }
  return;
  /* decompilation stopped at SEARCH_SURFACE */
}
