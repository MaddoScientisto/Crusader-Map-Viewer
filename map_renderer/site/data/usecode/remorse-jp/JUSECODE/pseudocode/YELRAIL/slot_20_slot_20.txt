function yelrail_slot_20() /* entry=2592 class_id=0x0a1e slot=0x20 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    ref, /* [BP+10h] type=0x69 */
    shotRef, /* [BP+12h] type=0x69 */
    vel, /* [BP+14h] type=0x69 */
    item, /* [BP-02h] type=0x24 */
    damage; /* [BP-04h] type=0x69 */

  set_info(0x0001, *(arg_06));
  set_info(1, *(arg_06));
  FREE.slot_2c(vel, shotRef);
  damage = retval;
  if (!damage) {
    return;
  }
  else if (((damage == 4) || (damage == 3)) || (damage == 10)) {
    if (UCMachine.rndRange(100, 0) > 50) {
      AudioProcess.playSFXCru(0x00bd, arg_06);
    }
    else {
      AudioProcess.playSFXCru(0x00bc, arg_06);
    }
    if ((Item.getShape(arg_06) == yelrail_shape_008a) || (Item.getShape(arg_06) == yelrail_shape_0088)) {
      if (!Item.getFrame(arg_06)) {
        Item.getPoint(UCMachine.rndRange(3, 1), arg_06);
      }
      return;
    }
    else if ((Item.getShape(arg_06) == yelrail_shape_008b) || (Item.getShape(arg_06) == yelrail_shape_0091)) {
      if (!Item.getFrame(arg_06)) {
        Item.getPoint(UCMachine.rndRange(2, 1), arg_06);
      }
      return;
    }
    else if ((Item.getShape(arg_06) == yelrail_shape_008c) || (Item.getShape(arg_06) == yelrail_shape_0092)) {
      if (!Item.getFrame(arg_06)) {
        Item.getPoint(1, arg_06);
      }
      return;
    }
    else {
      switch (Item.getShape(arg_06)) {
      case 141:
        switch (Item.getFrame(arg_06)) {
        case 0:
          Item.getPoint(4, arg_06);
          break;
        case 1:
          Item.getPoint(5, arg_06);
          break;
        case 2:
          Item.getPoint(7, arg_06);
          break;
        case 3:
          Item.getPoint(6, arg_06);
          break;
        }
        return;
      case 112:
        switch (Item.getFrame(arg_06)) {
        case 0:
          Item.getPoint(4, arg_06);
          break;
        case 1:
          Item.getPoint(5, arg_06);
          break;
        case 2:
          Item.getPoint(6, arg_06);
          break;
        case 3:
          Item.getPoint(7, arg_06);
          break;
        }
        return;
      case 147:
        switch (Item.getFrame(arg_06)) {
        case 0:
          Item.getPoint(5, arg_06);
          break;
        case 1:
          Item.getPoint(11, arg_06);
          break;
        case 2:
          Item.getPoint(10, arg_06);
          break;
        case 3:
          Item.getPoint(UCMachine.rndRange(7, 6), arg_06);
          break;
        case 4:
          Item.getPoint(UCMachine.rndRange(9, 8), arg_06);
          break;
        }
        return;
      }
    }
  }
  return;
}
