function telepad_slot_20() /* entry=477 class_id=0x01db slot=0x20 */
{
  set_info(1, 0);
  /* global_address */
  if (intrinsic_00cf()) {
    return;
  }
  else {
    /* global_address */
    /* global_address */
    spawn NPC.slot_24(pid);
    suspend;
    if (arg_10 != 69) {
      /* global_address */
      if (intrinsic_0011() != arg_10) {
        suspend;
        if (arg_10 == 40) {
          spawn FREE.slot_23(pid, 0x00000000);
          suspend;
          spawn FREE.slot_25(pid, 0x00000000);
          suspend;
          spawn FREE.slot_32(pid, 10, 0x00000000);
          suspend;
          /* global_address */
          if (intrinsic_0011() == 1) {
            /* str_to_ptr */
            /* free_stack_string */
            suspend;
          }
          else if (intrinsic_0011() == 13) {
            /* str_to_ptr */
            /* free_stack_string */
            suspend;
          }
          else if (intrinsic_0011() == 5) {
            /* str_to_ptr */
            /* free_stack_string */
            suspend;
            /* str_to_ptr */
            /* free_stack_string */
            suspend;
          }
          return;
        }
        if (arg_10 == 41) {
          spawn FREE.slot_32(pid, 10, 0x00000000);
          suspend;
          /* str_to_ptr */
          /* free_stack_string */
          suspend;
          spawn FREE.slot_32(pid, 10, 0x00000000);
          suspend;
          return;
        }
        if (arg_10) {
          suspend;
        }
        suspend;
      }
      else {
        suspend;
      }
    }
    /* global_address */
    spawn NPC.slot_23(pid);
    suspend;
    if (arg_12 == 69) {
      /* global_address */
      local_15 = Item.legal_create();
      /* global_address */
      /* global_address */
      for local_09 in nearby_items(shape=TELEPAD_RED, origin=global[0x003c]) {
        local_02 = intrinsic_0013(local_09);
        local_04 = Item.legal_create(local_09);
        local_05 = Item.getZ(local_09);
        /* loopnext */
      }
      for local_09 in nearby_items(shape=0x04ee, origin=global[0x003c]) {
        /* loopnext */
      }
      spawn FREE.slot_32(pid, 5, 0x00000000);
      suspend;
      local_07 = 0;
      while (local_07 <= 40) {
        spawn FREE.slot_32(pid, 10, 0x00000000);
        suspend;
        local_07 = (1 + local_07);
      }
      /* global_address */
      return;
    }
    else {
      /* global_address */
    }
  }
  return;
  /* decompilation stopped at SEARCH_SURFACE */
}
