function telepad_slot_20() /* entry=477 class_id=0x01db slot=0x20 */
{
  entry:
    set_info(1, 0);
    /* global_address */
    if intrinsic_00cf() goto block_0515;

  block_050f:
    goto block_0a93;

  block_0515:
    /* global_address */
    /* global_address */
    spawn NPC.slot_24(pid);
    suspend;
    if (arg_10 != 69) goto block_089b;

  block_055a:
    /* global_address */
    if (intrinsic_0011() != arg_10) goto block_087b;

  block_056d:
    suspend;
    if (arg_10 != 40) goto block_0778;

  block_059c:
    if (intrinsic_0011() == 1) {
      /* str_to_ptr */
      /* free_stack_string */
      suspend;
    }
    else if (intrinsic_0011() == 13) {
      /* str_to_ptr */
      /* free_stack_string */
      suspend;
    }
    else if (intrinsic_0011() == 5) {
      /* str_to_ptr */
      /* free_stack_string */
      suspend;
      /* str_to_ptr */
      /* free_stack_string */
      suspend;
    }

  block_0767:
    goto block_0a93;

  block_0778:
    if (arg_10 != 41) goto block_0834;

  block_0786:
    spawn FREE.slot_32(pid, 10, 0x00000000);
    suspend;
    /* str_to_ptr */
    /* free_stack_string */
    suspend;
    spawn FREE.slot_32(pid, 10, 0x00000000);
    suspend;
    goto block_0a93;

  block_0834:
    if arg_10 goto block_085e;

  block_083c:
    suspend;

  block_085e:
    suspend;
    goto block_089b;

  block_087b:
    suspend;

  block_089b:
    /* global_address */
    spawn NPC.slot_23(pid);
    suspend;
    if (arg_12 != 69) goto block_0a7c;

  block_08c9:
    /* global_address */
    local_15 = Item.legal_create();
    /* global_address */
    /* global_address */

  block_08c9_selector_00:
    /* loop_selector local_09 in nearby_items(shape=TELEPAD_RED, origin=global[0x003c]) */

  block_093f:
    if condition goto block_0970;

  block_0942:
    local_02 = intrinsic_0013(local_09);
    local_04 = Item.legal_create(local_09);
    local_05 = Item.getZ(local_09);
    /* loopnext */
    goto block_093f;

  block_0970_selector_00:
    for local_09 in nearby_items(shape=0x04ee, origin=global[0x003c]) {
      /* loopnext */
    }

  block_09a2:
    spawn FREE.slot_32(pid, 5, 0x00000000);
    suspend;
    local_07 = 0;

  block_0a07:
    while (local_07 > 40) {
      spawn FREE.slot_32(pid, 10, 0x00000000);
      suspend;
      local_07 = (1 + local_07);
    }

  block_0a6a:
    /* global_address */
    goto block_0a93;

  block_0a7c:
    /* global_address */

  block_0a93:
    return;

  /* decompilation stopped at SEARCH_SURFACE */
}
