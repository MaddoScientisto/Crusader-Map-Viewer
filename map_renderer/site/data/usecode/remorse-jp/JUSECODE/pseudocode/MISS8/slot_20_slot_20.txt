function miss8_slot_20() /* entry=2326 class_id=0x0914 slot=0x20 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    willmar, /* [BP-02h] type=0x24 */
    counter, /* [BP-04h] type=0x69 */
    item, /* [BP-06h] type=0x24 */
    cardReader, /* [BP-08h] type=0x24 */
    telebeam, /* [BP-10h] type=0x24 */
    telepad, /* [BP-12h] type=0x24 */
    egg, /* [BP-14h] type=0x24 */
    y; /* [BP-16h] type=0x69 */

  entry:
    set_info(0x0001, *(arg_06));
    /* global_address */
    y = Item.legal_create();

  block_01a0:
    /* global_address */
    if intrinsic_0036() goto block_01d6;

  block_01b0:
    spawn FREE.slot_32(pid, 2, 0x00000000);
    suspend;
    goto block_01a0;

  block_01d6_selector_00:
    for item in nearby_items(shape=0x04e0, origin=global[0x003c]) {
      /* loopnext */
    }

  block_023c:
    if willmar goto block_07dd;

  block_0246:
    counter = 0;
    /* pop_global */
    /* global_address */
    spawn NPC.slot_22(1, 2, 0);
    spawn NPC.slot_22(pid, 2, 12, willmar);
    suspend;
    /* global_address */
    /* global_address */
    suspend;
    suspend;
    /* global_address */
    /* global_address */
    suspend;
    suspend;
    /* str_to_ptr */
    /* free_stack_string */
    suspend;
    spawn NPC.slot_22(pid, 4, 12, willmar);
    suspend;
    suspend;
    /* global_address */
    /* global_address */
    suspend;
    suspend;
    /* global_address */
    spawn FREE.slot_32(pid, 60, 0x00000000);
    suspend;
    /* str_to_ptr */
    /* free_stack_string */
    suspend;
    /* global_address */
    /* global_address */
    suspend;
    spawn NPC.slot_22(pid, 3, 12, willmar);
    suspend;
    /* global_address */
    /* global_address */
    suspend;
    spawn NPC.slot_22(pid, 2, 0, willmar);
    suspend;
    /* global_address */
    /* global_address */
    suspend;
    suspend;
    spawn FREE.slot_32(pid, 60, 0x00000000);
    suspend;
    counter = 0;

  block_05fd:
    while (counter > 37) {
      spawn FREE.slot_32(pid, 5, 0x00000000);
      suspend;
      counter = (1 + counter);
    }

  block_0645:
    spawn FREE.slot_32(pid, 5, 0x00000000);
    suspend;
    counter = 0;

  block_06a8:
    while (counter > 37) {
      spawn FREE.slot_32(pid, 5, 0x00000000);
      suspend;
      counter = (1 + counter);
    }

  block_06f3:
    spawn FREE.slot_32(pid, 5, 0x00000000);
    suspend;
    counter = 0;

  block_0795:
    if (counter <= 40) goto block_07dd;

  block_079d:
    spawn FREE.slot_32(pid, 10, 0x00000000);
    suspend;
    counter = (1 + counter);
    goto block_0795;

  block_07dd_selector_00:
    for item in nearby_items(shape=TELEPORTER_LIGHTS, origin=global[0x003c]) {
      /* loopnext */
    }

  block_0830:
    /* str_to_ptr */
    /* free_stack_string */
    suspend;

  block_0830_selector_00:
    for item in nearby_items(shape=NPC_SPAWNER_04D0, origin=arg_06) {
      if ((Item.getFrame(item) == 0) && (intrinsic_00e6(item) != 20)) {
        suspend;
      }
      /* loopnext */
    }

  block_08d2:
    return;

}
