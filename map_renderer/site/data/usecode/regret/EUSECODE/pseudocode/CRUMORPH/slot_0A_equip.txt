function crumorph_equip() /* entry=794 class_id=0x0318 slot=0x0a */
{
  set_info(0x0211, *(arg_06));
  process_exclude();
  for local_02 in nearby_items(family=6, origin=arg_06) {
    if (Actor.isNPC(local_02)) {
      if (Intrinsic000D(local_02) == Item.getQLo(arg_06)) {
        if (Actor.isDead(local_02) == 0) {
          NPC.slot_2a(local_02);
          if (!retval) {
            return;
          }
          local_04 = Actor.getCurrentActivityNo(local_02);
          Actor.clrInCombat(local_02);
          Actor.setDefaultActivity0(1, local_02);
          Actor.setDefaultActivity1(1, local_02);
          Actor.setDefaultActivity2(1, local_02);
          Actor.setActivity(112, local_02);
          Actor.clrInCombat(local_02);
          spawn FREE.slot_3b(pid, 3, 0x00000000);
          suspend;
          spawn NPC.slot_28(pid, local_02);
          suspend;
          World.setControlledNPCNum(local_02);
          while (World.getControlledNPCNum() > 1) {
            spawn FREE.slot_3b(pid, 2, 0x00000000);
            suspend;
          }
          if (Actor.isDead(local_02) == 0) {
            spawn TRIGGER.slot_20(pid, 0, *(arg_06), arg_06);
            suspend;
          }
          else {
            spawn TRIGGER.slot_20(pid, 1, *(arg_06), arg_06);
            suspend;
          }
          return;
        }
      }
    }
  }
  return;
}
