function free_slot_49() /* entry=2591 class_id=0x0a1d slot=0x49 */
{
  set_info(1, 0);
  /* loopscr */
  /* loop */
  while (condition) {
    if (!intrinsic_014e(1, local_02)) {
      /* global_address */
      /* global_address */
      /* global_address */
      spawn FREE.slot_3b(pid, 10, 0x00000000);
      suspend;
    }
    /* loopnext */
  }
  /* word_to_dword */
  /* pop_result */
  return;
}
