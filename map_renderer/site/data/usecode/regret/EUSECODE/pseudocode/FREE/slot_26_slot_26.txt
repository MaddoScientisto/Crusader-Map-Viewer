function free_slot_26() /* entry=2591 class_id=0x0a1d slot=0x26 */
{
  set_info(0x0228, 0);
  /* pop_global */
  if (global[0x000c] > 1) {
    return;
  }
  else {
    suspend;
    while (true) {
      suspend;
      if (Kernel.getNumProcesses(0x0228, 0) > 1) {
        return;
      }
      /* global_address */
      /* global_address */
      if (intrinsic_003d(((intrinsic_0143() == 1) || (global[0x000c] == 0))) < 3) {
        return;
      }
      /* global_address */
      if (intrinsic_0030() > 0x012c) {
        /* global_address */
        /* global_address */
      }
      else {
        /* global_address */
        /* global_address */
        /* global_address */
      }
      /* global_address */
      /* global_address */
      /* global_address */
      /* global_address */
      spawn FREE.slot_3b(pid, 60, 0x00000000);
      suspend;
    }
  }
  return;
}
