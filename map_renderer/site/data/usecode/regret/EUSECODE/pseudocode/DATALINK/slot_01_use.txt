function datalink_use() /* entry=1238 class_id=0x04d4 slot=0x01 */
{
  set_info(0x0207, *(arg_06));
  process_exclude();
  /* pop_global */
  set_info(0, 1, 0);
  /* global_address */
  local_05 = intrinsic_015c();
  /* global_address */
  while (intrinsic_0060()) {
    spawn FREE.slot_3b(pid, 5, 0x00000000);
    suspend;
  }
  /* global_address */
  if (!intrinsic_0061()) {
    local_03 = 1;
    /* global_address */
    suspend;
    /* global_address */
    /* global_address */
    suspend;
  }
  spawn FREE.slot_21(pid, 0x00a7, 0x00000000);
  suspend;
  /* global_address */
  if (intrinsic_003d() != 25) {
    /* free_local_string */
    local_07 = "MISSION OBJECTIVE:^------------------^^";
  }
  if (global[0x0001] == 0) {
    /* global_address */
    /* global_address */
    if (intrinsic_003d((intrinsic_003d() == 1)) == 2) {
      if (!(global[0x0011])) {
        /* concat */
        /* concat */
        /* free_local_string */
        local_07 = "^Secure transport back to Earth and make contact with General Maxis.";
      }
      else if (!(global[0x0010])) {
        /* str_to_ptr */
        /* free_stack_string */
        /* concat */
        /* free_local_string */
        local_07 = "Destroy Plasma Turbine on level 5.";
      }
      else {
        /* concat */
        /* concat */
        /* free_local_string */
        local_07 = "to the LMC Lunar Mining Base.";
      }
    }
    else {
      /* global_address */
      /* global_address */
      if (intrinsic_003d((intrinsic_003d() == 3)) == 4) {
        /* concat */
        /* free_local_string */
        local_07 = "Locate elevator on level 4 to rendezvous with the rebels' colonel.";
      }
      else {
        /* global_address */
        /* global_address */
        if (intrinsic_003d((intrinsic_003d() == 5)) == 6) {
          if (!(global[0x0013])) {
            /* str_to_ptr */
            /* free_stack_string */
            /* concat */
            /* concat */
            /* free_local_string */
            local_07 = "^^Insert Trojan Horse program into TGT system.";
          }
          else {
            /* concat */
            /* concat */
            /* free_local_string */
            local_07 = "^Take the tram to rebels' rendezvous point.";
          }
        }
        else {
          /* global_address */
          /* global_address */
          if (intrinsic_003d((intrinsic_003d() == 7)) == 8) {
            if (!(global[0x0014])) {
              /* str_to_ptr */
              /* free_stack_string */
              /* concat */
              /* concat */
              /* free_local_string */
              local_07 = "^^Download rebel prisoner files from LMC security system.";
            }
            else {
              /* concat */
              /* concat */
              /* free_local_string */
              local_07 = "^Tram has been programmed to return to the rebel meeting area.";
            }
          }
          else {
            /* global_address */
            /* global_address */
            if (intrinsic_003d((intrinsic_003d() == 9)) == 10) {
              if (!(global[0x0015])) {
                /* str_to_ptr */
                /* free_stack_string */
                /* concat */
                /* free_local_string */
                local_07 = "Locate rebel prisoners^on level 4^and liberate them.";
              }
              else {
                /* concat */
                /* concat */
                /* free_local_string */
                local_07 = "^Tram has been programmed to return to rebel meeting area.";
              }
            }
            else {
              /* global_address */
              /* global_address */
              if (intrinsic_003d((intrinsic_003d() == 11)) == 12) {
                if (!(global[0x0016])) {
                  /* str_to_ptr */
                  /* free_stack_string */
                  /* concat */
                  /* concat */
                  /* free_local_string */
                  local_07 = "^^Place FusionPac on conveyor.^^Exit area quickly.";
                }
                else {
                  /* concat */
                  /* concat */
                  /* free_local_string */
                  local_07 = "^FusionPac will detonate shortly.";
                }
              }
              else {
                /* global_address */
                /* global_address */
                if (intrinsic_003d((intrinsic_003d() == 13)) == 14) {
                  if (!(global[0x0017])) {
                    /* str_to_ptr */
                    /* free_stack_string */
                    /* concat */
                    /* concat */
                    /* free_local_string */
                    local_07 = "^Disarm nerve gas^canister before it^contaminates O2^regeneration system.";
                  }
                  else {
                    /* concat */
                    /* concat */
                    /* free_local_string */
                    local_07 = "^Tram has been programmed to return to rebel meeting area.";
                  }
                }
                else {
                  /* global_address */
                  /* global_address */
                  if (intrinsic_003d((intrinsic_003d() == 15)) == 16) {
                    if (!(global[0x0018])) {
                      /* str_to_ptr */
                      /* free_stack_string */
                      /* concat */
                      /* concat */
                      /* free_local_string */
                      local_07 = "^^Destroy WEC troop transport before it lands at Lunar Base.";
                    }
                    else {
                      /* concat */
                      /* concat */
                      /* free_local_string */
                      local_07 = "^Tram has been programmed to return to rebel camp.";
                    }
                  }
                  else {
                    /* global_address */
                    /* global_address */
                    if (intrinsic_003d((intrinsic_003d() == 17)) == 18) {
                      if (!(global[0x0019])) {
                        /* str_to_ptr */
                        /* free_stack_string */
                        /* concat */
                        /* concat */
                        /* free_local_string */
                        local_07 = "^^Disarm self-destruction system before Lunar Base is destroyed.";
                      }
                      else {
                        /* concat */
                        /* concat */
                        /* free_local_string */
                        local_07 = "^Tram has been programmed to take you to LMC Headquarters.";
                      }
                    }
                    else {
                      /* global_address */
                      /* global_address */
                      /* global_address */
                      /* global_address */
                      if (intrinsic_003d((intrinsic_003d((intrinsic_003d((intrinsic_003d() == 19)) == 20)) == 21)) == 22) {
                        if (!(global[0x001a])) {
                          /* str_to_ptr */
                          /* free_stack_string */
                          /* concat */
                          /* concat */
                          /* free_local_string */
                          local_07 = "^Destroy objective with any means available.";
                        }
                        else {
                          /* concat */
                          /* concat */
                          /* free_local_string */
                          local_07 = "^Tram has been programmed to return to Rebel Headquarters.";
                        }
                      }
                      else {
                        /* global_address */
                        if (intrinsic_003d() == 25) {
                          /* free_local_string */
                          local_07 = "^REBEL CAMP^__________^^Replenish supplies.^^Talk to officer in charge for next mission.";
                        }
                        else {
                          /* concat */
                          /* concat */
                          /* free_local_string */
                          local_07 = "^^Head can only be destroyed by securing DetPacs to his ears.";
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
  else {
    spawn FLICTEST.slot_20(pid, global[0x0001], local_02);
    suspend;
  }
  if (local_03) {
    /* global_address */
    /* global_address */
    suspend;
    /* global_address */
    suspend;
  }
  /* loopscr */
  /* loopscr */
  /* loopscr */
  /* loopscr */
  /* loop */
  while (condition) {
    /* loopnext */
  }
  spawn FREE.slot_3b(pid, 2, 0x00000000);
  suspend;
  /* free_local_string */
  return;
}
