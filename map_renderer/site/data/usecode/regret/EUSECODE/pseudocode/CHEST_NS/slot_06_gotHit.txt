function chest_ns_gotHit() /* entry=1361 class_id=0x054f slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  FREE.slot_35(arg_12, arg_10);
  if (!retval) {
    return;
  }
  else {
    local_02 = Item.getFrame(arg_06);
    if (local_02 < 2) {
      spawn ITEM.slot_29(pid, 0x008c, arg_12, arg_10, arg_06);
      suspend;
      if (!process_result) {
        FREE.slot_20(1);
        if (retval) {
          Item.playSfxCru(38, arg_06);
        }
        else {
          Item.playSfxCru(39, arg_06);
        }
        return;
      }
      else {
        spawn ITEM.slot_2a(pid, 0, 0, arg_06);
        suspend;
        Item.setFrame((local_02 + 2), arg_06);
        ITEM.slot_24(0x1000, arg_06);
      }
    }
  }
  return;
}
