function fallingc_gotHit() /* entry=97 class_id=0x005f slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  while (condition) {
    if (Item.getShape(local_02) == 95) {
    }
    else if ((Item.getItemFamily(local_02) == 6) && (Actor.isNPC(local_02) == 0)) {
      spawn ITEM.slot_2a(pid, 1, 0, local_02);
      suspend;
    }
  }
  return;
}
