function d_lmc_kn_gotHit() /* entry=1545 class_id=0x0607 slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  FREE.slot_35(arg_12, arg_10);
  if ((retval != 4) || (Game.isViolenceEnabled() == 0)) {
    return;
  }
  else {
    while (condition) {
      if (Item.getShape(local_02) == 0x0111) {
        Item.push(local_02);
        Item.popToCoords(Item.getZ(arg_06), Item.getY(arg_06), Item.getX(arg_06), local_02);
      }
      else {
        Item.destroy(local_02);
      }
    }
    if (Item.getFrame(arg_06) >= 10) {
      Item.setFrame(UCMachine.rndRange(5, 0), arg_06);
    }
    Item.setShape(0x04e1, arg_06);
    Intrinsic003F();
  }
  return;
}
