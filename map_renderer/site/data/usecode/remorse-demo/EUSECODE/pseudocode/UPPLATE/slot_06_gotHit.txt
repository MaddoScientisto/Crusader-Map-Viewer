function upplate_gotHit() /* entry=1097 class_id=0x0447 slot=0x06 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    ref, /* [BP+10h] type=0x69 */
    vel, /* [BP+12h] type=0x69 */
    item; /* [BP-02h] type=0x24 */

  set_info(0x020c, *(arg_06));
  process_exclude();
  FREE.slot_2c(vel, ref);
  if (retval) {
    return;
  }
  else if (Item.getFrame(arg_06) != 2) {
    return;
  }
  else {
    item = ref;
    if ((Item.setFrame(arg_06) == 0) || (Actor.getLastAnimSet(*(arg_06), item) == 0)) {
      return;
    }
    else if (Item.getMapArray(arg_06) != 0) {
      Item.legal_create(1, arg_06);
      spawn TRIGGER.slot_20(pid, 0, *(arg_06), arg_06);
      suspend;
      spawn FREE.waitNTimerTicks(pid, 20, 0x00000000);
      suspend;
      Item.legal_create(0, arg_06);
      while (1) {
        if (Item.setFrame(arg_06) != 0) {
          spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
          suspend;
        }
        else {
          spawn TRIGGER.slot_20(pid, 1, *(arg_06), arg_06);
          suspend;
          return;
        }
      }
      return;
    }
    else {
      Item.legal_create(1, arg_06);
      spawn FREE.waitNTimerTicks(pid, 20, 0x00000000);
      suspend;
      Item.legal_create(0, arg_06);
    }
  }
  return;
}
