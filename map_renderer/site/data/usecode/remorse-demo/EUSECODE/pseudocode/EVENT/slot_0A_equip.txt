function event_equip() /* entry=867 class_id=0x0361 slot=0x0a */
{
  var
    referent, /* [BP+00h] type=0x69 */
    event, /* [BP+10h] type=0x69 */
    item, /* [BP-02h] type=0x24 */
    source, /* [BP-04h] type=0x24 */
    dest, /* [BP-06h] type=0x24 */
    door, /* [BP-08h] type=0x24 */
    wp, /* [BP-13h] type=0x24 */
    counter, /* [BP-15h] type=0x69 */
    counter2, /* [BP-17h] type=0x69 */
    n, /* [BP-19h] type=0x24 */
    link, /* [BP-21h] type=0x69 */
    cx, /* [BP-23h] type=0x69 */
    cy, /* [BP-25h] type=0x69 */
    ex, /* [BP-27h] type=0x69 */
    ey, /* [BP-29h] type=0x69 */
    time, /* [BP-31h] type=0x69 */
    op, /* [BP-33h] type=0x69 */
    opp, /* [BP-35h] type=0x69 */
    post1, /* [BP-37h] type=0x24 */
    post2, /* [BP-39h] type=0x24 */
    floor, /* [BP-41h] type=0x24 */
    dir, /* [BP-43h] type=0x69 */
    qHi, /* [BP-45h] type=0x69 */
    flicMan; /* [BP-47h] type=0x24 */

  set_info(0x0211, *(arg_06));
  process_exclude();
  link = Actor.setImmortal(arg_06);
  switch (event) {
  case 0:
    Item.getRangeIfVisible(Actor.getMap(&global[0x003c]), 0, Item.getY(arg_06), Item.getX(arg_06));
    return;
  case 1:
    while (Actor.doAnim(&global[0x003c])) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }
    Ultima8Engine.setAvatarInStasis(1);
    CameraProcess.getCameraX(&global[0x003c]);
    cx = CameraProcess.getCameraY();
    cy = Item.setMapArray();
    ex = Item.getX(arg_06);
    ey = Item.getY(arg_06);
    spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
    suspend;
    spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
    suspend;
    spawn TRIGGER.slot_20(pid, (1 + 0x0080), *(arg_06), arg_06);
    suspend;
    Actor.getHp(&global[0x003c]);
    Ultima8Engine.setAvatarInStasis(0);
    return;
  case 2:
    while (Actor.doAnim(&global[0x003c])) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }
    Ultima8Engine.setAvatarInStasis(1);
    spawn FREE.slot_3d(pid, Item.getY(arg_06), Item.getX(arg_06), 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
    suspend;
    return;
  case 3:
    spawn FREE.slot_3e(pid, 0x00000000);
    suspend;
    spawn TRIGGER.slot_20(pid, (1 + 0x0080), *(arg_06), arg_06);
    suspend;
    Ultima8Engine.setAvatarInStasis(0);
    return;
  case 5:
    Item.explode(0, 0, arg_06);
    return;
  case 6:
    Item.explode(0, 1, arg_06);
    return;
  case 7:
    Item.explode(0, 2, arg_06);
    return;
  case 8:
    counter = Actor.clrInCombat(arg_06);
    if (counter == 0) {
      spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
      suspend;
    }
    else {
      counter2 = Actor.setImmortal(arg_06);
      Item.fallProbably_03E(counter, arg_06);
      spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
      suspend;
      Item.fallProbably_03E(counter2, arg_06);
    }
    return;
  case 9:
    counter = Actor.setImmortal(arg_06);
    for item in nearby_items(family=1, origin=arg_06) {
      if (((counter == Actor.setImmortal(item)) && (Item.getMapArray(item) == 0)) && (*(arg_06) != item)) {
        Item.getNpcNum(Actor.clrInCombat(arg_06), item);
        Item.move(Intrinsic08A(arg_06), item);
      }
    }
    return;
  case 10:
    for item in nearby_items(shape=NUMBERS, origin=arg_06) {
      if (Actor.setImmortal(arg_06) == Actor.setImmortal(item)) {
        if (Item.getFrame(item) == 0) {
          source = item;
        }
      }
    }
    for item in nearby_items(shape=NUMBERS, origin=arg_06) {
      if (Actor.setImmortal(arg_06) == Actor.setImmortal(item)) {
        if (Item.getFrame(item) == 1) {
          dest = item;
        }
      }
    }
    if (source && dest) {
      wp = Item.getX(dest);
      local_11 = Item.getY(dest);
      local_09 = Item.getZ(dest);
      Item.create(0, GRENADE, item);
      Item.popToCoords((Item.getZ(source) + 60), Item.getY(source), Item.getX(source), item);
      Item.enterFastArea(1, 64, wp, item);
    }
    return;
  case 15:
    Item.getRangeIfVisible(Actor.getMap(&global[0x003c]), 0, Item.getY(arg_06), Item.getX(arg_06));
    AudioProcess.playSFXCru(*(arg_06));
    return;
  case 16:
    Item.getRangeIfVisible(Item.getX(Item.getY(Actor.getMap(&global[0x003c]), 0, &global[0x003c]), &global[0x003c]));
    AudioProcess.playSFXCru(global[0x003c]);
    return;
  case 22:
    cx = CameraProcess.getCameraY();
    cy = Item.setMapArray();
    AudioProcess.playSFXCru(54, arg_06);
    Item.getRangeIfVisible(Actor.getMap(&global[0x003c]), 0, Item.getY(arg_06), Item.getX(arg_06));
    while (Container.destroyContents(54, arg_06)) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }
    Item.getRangeIfVisible(Actor.getMap(&global[0x003c]), 0, cy, cx);
    return;
  case 40:
    if (!Item.getItemFamily(&global[0x003c])) {
      Ultima8Engine.setAvatarInStasis(1);
      while (Actor.doAnim(&global[0x003c])) {
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
      }
      suspend;
      Item.destroy(arg_06);
    }
    return;
  case 41:
    if (Item.getItemFamily((global[0x0029] == 1), &global[0x003c]) == 1) {
      return;
    }
    Ultima8Engine.setAvatarInStasis(1);
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    spawn NPC.slot_22(pid, 3, 0, &global[0x003c]);
    suspend;
    spawn NPC.slot_22(pid, 4, 14, &global[0x003c]);
    suspend;
    spawn NPC.slot_22(pid, 3, 12, &global[0x003c]);
    suspend;
    suspend;
    while (condition) {
      if (Actor.setImmortal(door) == 5) {
        spawn DOOR.slot_20(pid, door);
        suspend;
      }
    }
    for door in nearby_items(shape=0x04d9, origin=arg_06) {
      n = door;
    }
    spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
    suspend;
    playFlic(pid, 0x0096, 0x00fa, "10e", arg_06);
    suspend;
    Ultima8Engine.setAvatarInStasis(0);
    spawn FREE.waitNTimerTicks(pid, 120, 0x00000000);
    suspend;
    while (1) {
      op = Item.getItemFamily(&global[0x003c]);
      opp = Item.getItemFamily(n);
      if ((op == 1) || (opp == 1)) {
        return;
      }
      spawn TRIGGER.slot_20(pid, 0, *(arg_06), arg_06);
      suspend;
      spawn FREE.waitNTimerTicks(pid, 0x012c, 0x00000000);
      suspend;
    }
    return;
  case 45:
    time = Intrinsic08A(arg_06);
    while (1) {
      counter = 0;
      FREE.slot_29((Actor.clrInCombat(arg_06) == 0), *(arg_06), global[0x003c]);
      if (retval > 50) {
        return;
      }
      for item in nearby_items(family=6, origin=global[0x003c]) {
        if (Actor.isNPC(item)) {
          counter = (1 + counter);
        }
      }
      if (counter <= 4) {
        Item.pop((Actor.clrInCombat(arg_06) - 1), arg_06);
        spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
        suspend;
        spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
        suspend;
        spawn TRIGGER.slot_20(pid, (1 + 0x0080), *(arg_06), arg_06);
        suspend;
        spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
        suspend;
      }
      else {
        spawn FREE.waitNTimerTicks(pid, 120, 0x00000000);
        suspend;
      }
    }
    return;
  case 46:
    time = Intrinsic08A(arg_06);
    while (1) {
      counter = 0;
      FREE.slot_29((Actor.clrInCombat(arg_06) == 0), *(arg_06), global[0x003c]);
      if ((retval > 50) || (Item.isOnScreen(arg_06) == 1)) {
        return;
      }
      for item in nearby_items(family=6, origin=global[0x003c]) {
        if (item < 0x00ff) {
          counter = (1 + counter);
        }
      }
      if (counter <= 4) {
        Item.pop((Actor.clrInCombat(arg_06) - 1), arg_06);
        spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
        suspend;
        spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
        suspend;
        spawn TRIGGER.slot_20(pid, (1 + 0x0080), *(arg_06), arg_06);
        suspend;
        spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
        suspend;
      }
      else {
        spawn FREE.waitNTimerTicks(pid, 120, 0x00000000);
        suspend;
      }
    }
    return;
  case 47:
    time = Intrinsic08A(arg_06);
    counter2 = Actor.clrInCombat(arg_06);
    while (1) {
      if (Actor.clrInCombat(arg_06) == 1) {
        counter = 1;
        while (counter <= counter2) {
          Item.fallProbably_03E(counter, arg_06);
          spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
          suspend;
          spawn FREE.waitNTimerTicks(pid, (time * 3), 0x00000000);
          suspend;
          spawn TRIGGER.slot_20(pid, (1 + 0x0080), *(arg_06), arg_06);
          suspend;
          spawn FREE.waitNTimerTicks(pid, (time * 3), 0x00000000);
          suspend;
          counter = (1 + counter);
        }
        Item.pop(counter2, arg_06);
      }
      else if (Actor.clrInCombat(arg_06) > 1) {
        counter = 1;
        while (counter <= counter2) {
          Item.fallProbably_03E(counter, arg_06);
          spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
          suspend;
          spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
          suspend;
          spawn TRIGGER.slot_20(pid, (1 + 0x0080), *(arg_06), arg_06);
          suspend;
          counter = (1 + counter);
        }
      }
      else {
        counter = 1;
        while (counter <= counter2) {
          Item.fallProbably_03E(counter, arg_06);
          spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
          suspend;
          spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
          suspend;
          counter = (1 + counter);
        }
        return;
      }
    }
    return;
  case 48:
    time = Intrinsic08A(arg_06);
    counter2 = Actor.clrInCombat(arg_06);
    counter = 1;
    while (counter <= counter2) {
      Item.fallProbably_03E(counter, arg_06);
      spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
      suspend;
      spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
      suspend;
      counter = (1 + counter);
    }
    counter = 1;
    while (counter <= counter2) {
      Item.fallProbably_03E((counter2 - counter), arg_06);
      spawn TRIGGER.slot_20(pid, (1 + 0x0080), *(arg_06), arg_06);
      suspend;
      spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
      suspend;
      counter = (1 + counter);
    }
    Item.fallProbably_03E(link, arg_06);
    return;
  case 49:
    time = Intrinsic08A(arg_06);
    counter2 = Actor.clrInCombat(arg_06);
    while (1) {
      if (Actor.clrInCombat(arg_06)) {
        counter = 1;
        while (counter <= counter2) {
          Item.fallProbably_03E(counter, arg_06);
          spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
          suspend;
          spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
          suspend;
          spawn TRIGGER.slot_20(pid, (1 + 0x0080), *(arg_06), arg_06);
          suspend;
          counter = (1 + counter);
        }
      }
      else {
        counter = 1;
        while (counter <= counter2) {
          Item.fallProbably_03E(counter, arg_06);
          spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
          suspend;
          spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
          suspend;
          counter = (1 + counter);
        }
        return;
      }
    }
    return;
  case 50:
    while (condition) {
      if (Actor.setImmortal(door) == 100) {
        spawn DOOR.slot_20(pid, door);
        suspend;
      }
    }
    return;
  case 51:
    for item in nearby_items(shape=GRATE_FLOOR_GRAY, origin=arg_06) {
      dir = Actor.clrInCombat(item);
      floor = item;
    }
    for item in selector_0x42(arg0=0x01c4, arg1=0x0186, arg2=(10 * 32), origin=floor) {
      post1 = item;
    }
    for item in selector_0x42(arg0=0x01c5, arg1=0x0184, arg2=(10 * 32), origin=floor) {
      post2 = item;
    }
    if (post1 && post2) {
      AudioProcess.playSFXCru(0x00f4, &global[0x003c]);
      spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
      suspend;
      Item.setQLo(0x00f3, &global[0x003c]);
      if (dir) {
        Item.legal_create(0, post1);
        Item.legal_create(0, post2);
        counter = 1;
        while (counter <= 4) {
          Item.legal_create(counter, post1);
          Item.legal_create(counter, post2);
          Item.getQHi(0x0093, post1);
          spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
          suspend;
          counter = (1 + counter);
        }
      }
      else {
        counter = 0;
        while (counter <= 3) {
          Item.legal_create((7 - counter), post1);
          Item.legal_create((7 - counter), post2);
          Item.getQHi(0x0093, post1);
          spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
          suspend;
          counter = (1 + counter);
        }
      }
      spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
      suspend;
      Item.legal_create(0, post1);
      Item.legal_create(0, post2);
      Item.getQHi(0x0093, post1);
    }
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    spawn TRIGGER.slot_20(pid, 0, *(arg_06), arg_06);
    suspend;
    for item in selector_0x42(arg0=0x0193, arg1=0x01c7, arg2=(10 * 32), origin=floor) {
      door = item;
      spawn DOOR.slot_20(pid, door);
      suspend;
    }
    spawn TRIGGER.slot_20(pid, 1, *(arg_06), arg_06);
    suspend;
    return;
  case 52:
    time = Intrinsic08A(arg_06);
    counter2 = Actor.clrInCombat(arg_06);
    while (1) {
      for n in nearby_items(family=6, origin=arg_06) {
        if (Actor.isNPC(n)) {
          if (Actor.GetNPCDataField0x63_00B(n) == Actor.setImmortal(arg_06)) {
            if (!Item.getItemFamily(n)) {
              AudioProcess.playSFXCru(0x0099, n);
              Item.getQuality(5, n);
            }
          }
        }
        spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
        suspend;
      }
      return;
    }
    return;
  case 53:
    for n in nearby_items(shape=wallgun_shape_04c8, origin=arg_06) {
      spawn FREE.waitNTimerTicks(pid, 1, 0x00000000);
      suspend;
      Item.create(4, wallgun_shape_04c8, item);
      Item.pop(44, item);
      Item.move(4, item);
      Item.popToCoords(Item.getZ(n), Item.getY(n), Item.getX(n), item);
      Item.destroy(n);
    }
    return;
  case 54:
    for n in nearby_items(shape=wallgun_shape_04c8, origin=arg_06) {
      Item.pop(100, n);
      Item.move(6, n);
      Item.hurl(n);
    }
    return;
  case 55:
    if (!(global[0x0038])) {
      return;
    }
    time = Intrinsic08A(arg_06);
    while (1) {
      counter = 0;
      Item.pop((Actor.clrInCombat(arg_06) - 1), arg_06);
      spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
      suspend;
      spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
      suspend;
      spawn TRIGGER.slot_20(pid, (1 + 0x0080), *(arg_06), arg_06);
      suspend;
      spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
      suspend;
      if (Actor.clrInCombat(arg_06) == 0) {
        return;
      }
    }
    return;
  case 75:
    for item in nearby_items(family=6, origin=arg_06) {
      if (Actor.setImmortal(item) == Actor.setImmortal(arg_06)) {
        suspend;
      }
    }
    return;
  case 85:
    time = (PaletteFaderProcess.setPalToAllBlack(arg_06) * 5);
    while (1) {
      counter = 0;
      FREE.slot_29((Actor.clrInCombat(arg_06) == 0), *(arg_06), global[0x003c]);
      if (retval > 50) {
        return;
      }
      for item in nearby_items(family=6, origin=global[0x003c]) {
        if (Actor.isNPC(item)) {
          counter = (1 + counter);
        }
      }
      if (counter <= 4) {
        Item.pop((Actor.clrInCombat(arg_06) - 1), arg_06);
        spawn TRIGGER.slot_20(pid, (0 + 0x0080), *(arg_06), arg_06);
        suspend;
        spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
        suspend;
        spawn TRIGGER.slot_20(pid, (1 + 0x0080), *(arg_06), arg_06);
        suspend;
        spawn FREE.waitNTimerTicks(pid, time, 0x00000000);
        suspend;
      }
      else {
        spawn FREE.waitNTimerTicks(pid, 120, 0x00000000);
        suspend;
      }
    }
    return;
  case 88:
    Ultima8Engine.setAvatarInStasis(1);
    MusicProcess.musicStop();
    setSomeMovieGlobal();
    spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
    suspend;
    playFlic092();
    clearSomeMovieGlobal(pid, 0, 0, "o01", arg_06);
    suspend;
    Game.playCredits();
    Kernel.getCurrentKeyDown();
    return;
  case 97:
    Item.getRangeIfVisible(Actor.getMap(&global[0x003c]), 0, Item.getY(arg_06), Item.getX(arg_06));
    while (!MainActor.teleportToEgg()) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }
    Item.getRangeIfVisible(Item.getX(Item.getY(Actor.getMap(&global[0x003c]), 0, &global[0x003c]), &global[0x003c]));
    return;
  case 98:
    for item in nearby_items(shape=EVENT_CONTROLLER, origin=arg_06) {
      Item.pop(1, item);
    }
    return;
  case 99:
    for item in nearby_items(shape=EVENT_CONTROLLER, origin=arg_06) {
      Item.pop(0, item);
    }
    return;
  case 100:
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    suspend;
    return;
  case 200:
    if (global[0x0022] == 0) {
      Actor.setDead(0x0096, 0x00c8, "mva3b", arg_06);
      suspend;
      spawn FREE.waitNTimerTicks(pid, 20, 0x00000000);
      suspend;
      spawn FREE.slot_26(pid, "3h", 0x00000000);
      suspend;
    }
    return;
  case 201:
    qHi = Actor.clrInCombat(arg_06);
    if (qHi < 4) {
      qHi = (1 + qHi);
      Item.pop(qHi, arg_06);
    }
    else {
      spawn TRIGGER.slot_20(pid, (1 + 0x0080), *(arg_06), arg_06);
      suspend;
    }
    return;
  case 212:
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    playFlic(pid, 0x0096, 0x00c8, "13d", arg_06);
    suspend;
    Ultima8Engine.setAvatarInStasis(1);
    resetVargasHealthTo500();
    return;
  case 213:
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    playFlic(pid, 0x0096, 0x00c8, "13e", arg_06);
    suspend;
    return;
  case 230:
    qHi = Actor.clrInCombat(arg_06);
    if (qHi < 2) {
      qHi = (1 + qHi);
      Item.pop(qHi, arg_06);
    }
    else {
      Item.andStatus();
    }
    return;
  case 250:
    spawn FLICTEST.slot_20(pid, flicMan);
    suspend;
    return;
  }
  return;
}
