function reb_coup_enterFastArea() /* entry=847 class_id=0x034d slot=0x0f */
{
  var
    referent, /* [BP+00h] type=0x69 */
    counter; /* [BP-02h] type=0x69 */

  entry:
    set_info(0x021b, *(arg_06));
    process_exclude();

  while (true) {
    suspend;
    spawn FREE.waitNTimerTicks(UCMachine.rndRange(pid, 60, 30), 0x00000000);
    suspend;
    counter = 0;
    while (counter <= 4) {
      Item.legal_create(counter, arg_06);
      spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
      suspend;
      counter = (1 + counter);
    }
    spawn FREE.waitNTimerTicks(UCMachine.rndRange(pid, 60, 30), 0x00000000);
    suspend;
    counter = 0;
    while (counter <= 4) {
      Item.legal_create((4 - counter), arg_06);
      spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
      suspend;
      counter = (1 + counter);
    }
  }

  return;

}
