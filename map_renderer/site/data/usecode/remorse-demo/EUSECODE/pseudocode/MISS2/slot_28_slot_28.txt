function miss2_slot_28() /* entry=2320 class_id=0x090e slot=0x28 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    counter, /* [BP-02h] type=0x69 */
    item, /* [BP-04h] type=0x24 */
    body, /* [BP-06h] type=0x24 */
    gun, /* [BP-08h] type=0x24 */
    door, /* [BP-10h] type=0x24 */
    door1, /* [BP-12h] type=0x24 */
    door2, /* [BP-14h] type=0x24 */
    door3, /* [BP-16h] type=0x24 */
    egg, /* [BP-18h] type=0x24 */
    hoffman, /* [BP-20h] type=0x24 */
    x, /* [BP-22h] type=0x69 */
    y, /* [BP-24h] type=0x69 */
    pathEgg; /* [BP-26h] type=0x24 */

  entry:
    set_info(0x0001, *(arg_06));
    counter = 0;
    egg = *(arg_06);
    set_info(1, 0);

  block_0cd5:
    while (Actor.doAnim(&global[0x003c])) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }

  block_0d0b:
    if !AudioProcess.stopSFXCru(&global[0x003c]) goto block_0d4b;

  block_0d1b:
    suspend;

  block_0d4b:
    hoffman = 0;
    /* loopscr */
    /* loop */

  block_0d68:
    while (condition) {
      if ((Item.getZ(item) > 96) && (Item.getZ(item) < 0x0096)) {
        x = Item.getX(item);
        y = Item.getY(item);
        if ((((x > 0x522f) && (x < 0x53bf)) && (y < 0x448e)) && (y > 0x407f)) {
        }
      }
    }

  block_0e2a_selector_00:
    /* loop_selector item in nearby_items(shape=0x04d9, origin=global[0x003c]) */

  block_0e4a:
    if !condition goto block_0e98;

  block_0e4d:
    goto block_0e98;

  block_0e94:
    goto block_0e4a;

  block_0e98:
    if !hoffman goto block_1ced;

  block_0ea2_selector_00:
    for item in nearby_items(shape=door_shape_0046, origin=global[0x003c]) {
      FREE.slot_29(item, global[0x003c]);
      while ((retval > 3) && (counter != 10)) {
        spawn NPC.slot_22(MainActor.addItemCru(pid, 1, (Item.getY(item) - 32), Item.getX(Item.getY((Item.getX(item) - 16), &global[0x003c]), &global[0x003c])), &global[0x003c]);
        suspend;
        counter = (1 + counter);
      }
      door = item;
      spawn DOOR.slot_20(pid, door);
      suspend;
    }

  block_0f73:
    spawn NPC.slot_22(pid, 3, 4, &global[0x003c]);
    suspend;
    spawn NPC.slot_22(MainActor.addItemCru(pid, 3, Item.getY(hoffman), Item.getX(Item.getY(Item.getX(hoffman), &global[0x003c]), &global[0x003c])), &global[0x003c]);
    suspend;
    suspend;

  block_0f73_selector_00:
    /* loop_selector item in nearby_items(shape=NUMBERS, origin=global[0x003c]) */

  block_103a:
    if !condition goto block_104e;

  block_103d:
    pathEgg = item;
    goto block_104e;

  block_104a:
    goto block_103a;

  block_1050:
    while ((retval > 2) && (counter != 20)) {
      spawn NPC.slot_22(MainActor.addItemCru(pid, 1, Item.getY(pathEgg), Item.getX(Item.getY(Item.getX(pathEgg), &global[0x003c]), &global[0x003c])), &global[0x003c]);
      suspend;
      counter = (1 + counter);
    }

  block_10d1:
    suspend;
    /* str_to_ptr */
    /* free_stack_string */
    suspend;
    spawn FREE.waitNTimerTicks(pid, 10, 0x00000000);
    suspend;
    suspend;
    suspend;
    suspend;
    suspend;
    suspend;

  block_10d1_selector_00:
    for item in nearby_items(shape=SPANEL, origin=hoffman) {
      if (Item.getQHi(item) == 13) {
        suspend;
        suspend;
      }
    }

  block_12a1:
    suspend;
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;

  block_12a1_selector_00:
    for item in nearby_items(shape=0x04d5, origin=global[0x003c]) {
    }

  block_1362:
    spawn FREE.waitNTimerTicks(pid, 45, 0x00000000);
    suspend;
    spawn MISS2.slot_20(pid, 1, 0x00000000);
    suspend;
    spawn MISS2.slot_23(pid, 0x00000000);
    suspend;
    gun = 0;

  block_1362_selector_00:
    for item in nearby_items(shape=wallgun_shape_04c8, origin=global[0x003c]) {
      gun = item;
    }

  block_1405:
    if !gun goto block_1432;

  block_140f:
    spawn MISS2.slot_25(pid, 1, 0x00000000);
    suspend;

  block_1432:
    spawn FREE.waitNTimerTicks(pid, 15, 0x00000000);
    suspend;
    door = 0;
    door1 = 0;
    door2 = 0;
    door3 = 0;

  block_1432_selector_00:
    for item in nearby_items(shape=PRISON_BARS_DOOR, origin=gun) {
      if (!Item.getQHi(item)) {
        door = item;
      }
      else if (Item.getQHi(item) == 1) {
        door1 = item;
      }
      else if (Item.getQHi(item) == 2) {
        door2 = item;
      }
      else if (Item.getQHi(item) == 3) {
        door3 = item;
      }
    }

  block_14f9:
    if !(((door && door1) && door2) && door3) goto block_1a3e;

  block_1515:
    spawn MISS2.slot_26(pid, 0, 0x00000000);
    suspend;
    spawn DOOR.slot_20(pid, door);
    suspend;
    spawn MISS2.slot_27(pid, gun, 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 3, 0x00000000);
    suspend;

  block_1515_selector_00:
    for item in nearby_items(shape=PRISONER_2, origin=door) {
      spawn FREE.waitNTimerTicks(pid, 15, 0x00000000);
      suspend;
    }

  block_1647:
    while (Item.getFrame(gun) != 4) {
      spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
      suspend;
    }

  block_169c:
    spawn DOOR.slot_20(pid, door1);
    suspend;
    spawn MISS2.slot_27(pid, gun, 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 3, 0x00000000);
    suspend;

  block_169c_selector_00:
    for item in nearby_items(shape=PRISONER_1, origin=door1) {
      spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
      suspend;
    }

  block_17a9:
    spawn MISS2.slot_26(pid, 1, 0x00000000);
    suspend;
    spawn DOOR.slot_20(pid, door2);
    suspend;
    spawn MISS2.slot_27(pid, gun, 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 6, 0x00000000);
    suspend;

  block_17a9_selector_00:
    for item in nearby_items(shape=PRISONER_1, origin=door2) {
      spawn FREE.waitNTimerTicks(pid, 15, 0x00000000);
      suspend;
    }

  block_18dd:
    while (Item.getFrame(gun) != 4) {
      spawn FREE.waitNTimerTicks(pid, 5, 0x00000000);
      suspend;
    }

  block_1932:
    spawn DOOR.slot_20(pid, door3);
    suspend;
    spawn MISS2.slot_27(pid, gun, 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 6, 0x00000000);
    suspend;

  block_1932_selector_00:
    for item in nearby_items(shape=PRISONER_3, origin=door3) {
      spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
      suspend;
    }

  block_1a3e:
    spawn MISS2.slot_25(pid, 0, 0x00000000);
    suspend;
    spawn MISS2.slot_23(pid, 0x00000000);
    suspend;
    spawn MISS2.slot_20(pid, 0, 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
    suspend;
    suspend;
    suspend;
    suspend;
    suspend;
    suspend;
    suspend;
    suspend;
    suspend;
    suspend;
    suspend;

  block_1a3e_selector_00:
    for item in nearby_items(shape=PANELNS, origin=hoffman) {
      if (Item.getQHi(item) == 13) {
        suspend;
      }
    }

  block_1ced:
    return;

}
