function rechflns_gotHit() /* entry=314 class_id=0x0138 slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  local_02 = arg_10;
  if (((arg_10 == 1) && (Item.getFrame(arg_06) == 0)) && (intrinsic_00ea(arg_06) == 0)) {
    if (!intrinsic_005a(*(arg_06), local_02)) {
      return;
    }
    else {
      /* global_address */
      while (intrinsic_0036()) {
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
      }
      /* global_address */
      /* global_address */
      suspend;
      for local_02 in nearby_items(shape=0x0136, origin=arg_06) {
        /* loopnext */
      }
      spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
      suspend;
      while (intrinsic_00ef(0x0254, global[0x003c])) {
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
      }
      for local_02 in nearby_items(shape=0x0136, origin=arg_06) {
        /* loopnext */
      }
    }
  }
  return;
  /* decompilation stopped at SEARCH_SURFACE */
}
