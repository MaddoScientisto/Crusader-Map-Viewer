function trigger_slot_29() /* entry=2586 class_id=0x0a18 slot=0x29 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    ref, /* [BP+10h] type=0x69 */
    qHi, /* [BP+12h] type=0x69 */
    npcNum, /* [BP+14h] type=0x69 */
    mapNum, /* [BP+16h] type=0x69 */
    baseLink, /* [BP+18h] type=0x69 */
    searchtype, /* [BP+20h] type=0x69 */
    scmd, /* [BP-02h] type=0x69 */
    cmd, /* [BP-04h] type=0x69 */
    a, /* [BP-06h] type=0x69 */
    b, /* [BP-08h] type=0x69 */
    c, /* [BP-10h] type=0x69 */
    d, /* [BP-12h] type=0x69 */
    aitem, /* [BP-14h] type=0x24 */
    npc, /* [BP-16h] type=0x24 */
    e, /* [BP-18h] type=0x69 */
    f, /* [BP-20h] type=0x69 */
    g, /* [BP-22h] type=0x69 */
    counter, /* [BP-24h] type=0x69 */
    h, /* [BP-26h] type=0x69 */
    i, /* [BP-28h] type=0x69 */
    k; /* [BP-30h] type=0x69 */

  set_info(0x021b, 0);
  aitem = ref;
  cmd = (mapNum & 3);
  if (cmd == 0) {
    for npc in nearby_items(shape=searchtype, origin=aitem) {
      if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
        a = (qHi / 16);
        b = (qHi & 15);
        if (a == 0) {
          e = Actor.GetNPCDataField0x4_0DD(npc);
          suspend;
          spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
          suspend;
          f = Actor.GetNPCDataField0x4_0DD(npc);
        }
        else {
          g = Actor.GetNPCDataField0x4_0DD(npc);
          suspend;
          spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
          suspend;
          if (Item.getItemFamily(npc) == 1) {
            return;
          }
          counter = 1;
          while (counter <= a) {
            if (Item.getItemFamily(npc) == 0) {
              suspend;
            }
            spawn FREE.waitNTimerTicks(pid, 1, 0x00000000);
            suspend;
            counter = (1 + counter);
          }
          h = Actor.GetNPCDataField0x4_0DD(npc);
          i = Actor.setDefaultActivity0(npc);
        }
      }
    }
    return;
  }
  else if (cmd == 1) {
    for npc in nearby_items(shape=searchtype, origin=aitem) {
    }
    return;
  }
  else if (cmd == 2) {
    for npc in nearby_items(shape=searchtype, origin=aitem) {
    }
    return;
  }
  else if (cmd == 3) {
    scmd = (qHi & 7);
    a = (qHi / 8);
    if (scmd == 0) {
      for npc in nearby_items(shape=searchtype, origin=aitem) {
        if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
          if (a == 1) {
          }
          else if (a == 2) {
          }
          else if (a == 3) {
          }
        }
      }
    }
    else if (scmd == 1) {
      for npc in nearby_items(shape=searchtype, origin=aitem) {
        if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
          spawn FREE.waitNTimerTicks(pid, 1, 0x00000000);
          suspend;
        }
      }
    }
    else if (scmd == 2) {
      for npc in nearby_items(shape=searchtype, origin=aitem) {
        if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
          spawn FREE.waitNTimerTicks(pid, 1, 0x00000000);
          suspend;
        }
      }
    }
    else if (scmd == 3) {
      for npc in nearby_items(shape=searchtype, origin=aitem) {
        if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
          spawn FREE.waitNTimerTicks(pid, 1, 0x00000000);
          suspend;
        }
      }
    }
    else if (scmd == 4) {
      for npc in nearby_items(shape=searchtype, origin=aitem) {
        if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
          k = a;
        }
      }
    }
    else if (scmd == 5) {
      for npc in nearby_items(shape=searchtype, origin=aitem) {
        if (Actor.GetNPCDataField0x63_00B(npc) == baseLink) {
          spawn NPC.equip(a, npc);
        }
      }
    }
    return;
  }
  return;
}
