function miss1egg_slot_22() /* entry=2319 class_id=0x090d slot=0x22 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    buster, /* [BP+10h] type=0x69 */
    rubberSuit, /* [BP-02h] type=0x24 */
    counter; /* [BP-04h] type=0x69 */

  set_info(1, buster);
  FREE.slot_33(buster, 0x0205);
  rubberSuit = buster;
  if (Item.getItemFamily(rubberSuit)) {
    return;
  }
  else {
    suspend;
    spawn NPC.slot_2a(pid, 0x00b4, rubberSuit);
    suspend;
    if (Item.getItemFamily(rubberSuit)) {
      return;
    }
    else {
      suspend;
      while (((Actor.setActivity(rubberSuit) == 0) && (Item.getItemFamily(rubberSuit) == 0)) && (counter < 4)) {
        suspend;
        counter = (1 + counter);
      }
      if (Item.getItemFamily(rubberSuit)) {
        return;
      }
      else {
        suspend;
        if ((Actor.setActivity(rubberSuit) == 0) && (Item.getItemFamily(rubberSuit) == 0)) {
          suspend;
        }
        spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
        suspend;
        if ((Actor.setActivity(rubberSuit) == 0) || (Item.getItemFamily(rubberSuit) == 0)) {
          spawn FREE.slot_3b(0x00000000);
        }
      }
    }
  }
  return;
}
