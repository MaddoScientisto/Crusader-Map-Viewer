function rechflew_gotHit() /* entry=348 class_id=0x015a slot=0x06 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    ref, /* [BP+10h] type=0x69 */
    vel, /* [BP+12h] type=0x69 */
    item; /* [BP-02h] type=0x24 */

  set_info(0x020c, *(arg_06));
  process_exclude();
  item = ref;
  if (((ref == 1) && (Item.getFrame(arg_06) == 0)) && (Item.getQLo(arg_06) == 0)) {
    if (!Actor.getLastAnimSet(*(arg_06), item)) {
      return;
    }
    else {
      while (Actor.doAnim(&global[0x003c])) {
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
      }
      suspend;
      for item in nearby_items(shape=0x0158, origin=arg_06) {
      }
      spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
      suspend;
      while (Kernel.getNumProcesses(0x0254, global[0x003c])) {
        spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
        suspend;
      }
      for item in nearby_items(shape=0x0158, origin=arg_06) {
      }
    }
  }
  return;
}
