function fight_n3_gotHit() /* entry=386 class_id=0x0180 slot=0x06 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    ref, /* [BP+10h] type=0x69 */
    vel, /* [BP+12h] type=0x69 */
    item; /* [BP-02h] type=0x24 */

  set_info(0x020c, *(arg_06));
  process_exclude();
  if (Item.getStatus(arg_06) & 0x1000) {
    return;
  }
  else {
    FREE.slot_2c(vel, ref);
    if (!retval) {
      return;
    }
    else {
      spawn ITEM.slot_27(pid, 0x00c8, vel, ref, arg_06);
      suspend;
      if (!process_result) {
        FREE.slot_20(1);
        if (retval) {
        }
        return;
      }
      else {
        while (condition) {
          ITEM.slot_23(0x1000, item);
          if (!Item.getFrame(item)) {
            if ((Item.getShape(item) == 0x017f) || (Item.getShape(item) == 0x0141)) {
              spawn ITEM.slot_28(pid, 0, 2, item);
              suspend;
            }
            else {
              spawn ITEM.slot_28(pid, 0, 1, item);
              suspend;
            }
          }
        }
      }
    }
  }
  return;
}
