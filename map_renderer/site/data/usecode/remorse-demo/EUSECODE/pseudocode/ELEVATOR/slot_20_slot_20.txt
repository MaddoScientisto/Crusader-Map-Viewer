function elevator_slot_20() /* entry=544 class_id=0x021e slot=0x20 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    dir, /* [BP+10h] type=0x69 */
    mapNum, /* [BP+12h] type=0x69 */
    eggNum, /* [BP+14h] type=0x69 */
    post1, /* [BP-02h] type=0x24 */
    post2, /* [BP-04h] type=0x24 */
    item, /* [BP-06h] type=0x24 */
    counter, /* [BP-08h] type=0x69 */
    door, /* [BP-10h] type=0x24 */
    door1; /* [BP-12h] type=0x24 */

  set_info(1, 0);
  for item in selector_0x42(arg0=0x0189, arg1=0x0289, arg2=(10 * 32), origin=arg_06) {
    door = item;
  }
  if (!door) {
    return;
  }
  else if (Item.getShape(door) == ELEVATOR_DOOR_SEGMENT) {
    suspend;
    suspend;
    suspend;
    suspend;
  }
  else {
    suspend;
    suspend;
    suspend;
    suspend;
  }
  Kernel.resetRef(Item.getZ(arg_06), (Item.getY(arg_06) - 16), (Item.getX(arg_06) - 16), &global[0x003c]);
  spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
  suspend;
  spawn DOOR.slot_21(pid, 2, door);
  suspend;
  for item in selector_0x42(arg0=0x01c4, arg1=0x0186, arg2=(20 * 32), origin=arg_06) {
    post1 = item;
  }
  for item in selector_0x42(arg0=0x01c5, arg1=0x0184, arg2=(20 * 32), origin=arg_06) {
    post2 = item;
  }
  if (post1 && post2) {
    spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
    suspend;
    PaletteFaderProcess.fadeToBlack(&global[0x003c]);
    AudioProcess.playSFXCru(0x00f4, &global[0x003c]);
    Item.legal_create(4, post1);
    Item.legal_create(4, post2);
    AudioProcess.playSFXCru(0x0093, arg_06);
    while (Container.destroyContents(0x00f4, &global[0x003c])) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }
    AudioProcess.playSFXCru(0x00f5, &global[0x003c]);
    if (dir) {
      counter = 5;
      while (counter <= 7) {
        Item.legal_create(counter, post1);
        Item.legal_create(counter, post2);
        Item.getQHi(0x0093, arg_06);
        spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
        suspend;
        counter = (1 + counter);
      }
      Item.legal_create(0, post1);
      Item.legal_create(0, post2);
      Item.getQHi(0x0093, arg_06);
      spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
      suspend;
    }
    else {
      counter = 1;
      while (counter <= 4) {
        Item.legal_create((4 - counter), post1);
        Item.legal_create((4 - counter), post2);
        Item.getQHi(0x0093, arg_06);
        spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
        suspend;
        counter = (1 + counter);
      }
    }
  }
  spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
  suspend;
  suspend;
  spawn FREE.slot_3c(pid, 0x00000000);
  suspend;
  if (Actor.getMap(mapNum, &global[0x003c])) {
    MainActor.teleportToEgg();
  }
  suspend;
  for item in nearby_items(shape=GRATE_FLOOR_GRAY, origin=global[0x003c]) {
    Kernel.resetRef(Item.getZ(item), (Item.getY(item) - 16), (Item.getX(item) - 16), &global[0x003c]);
    suspend;
    FREE.slot_33(12, item);
  }
  if (eggNum == 18) {
    suspend;
  }
  suspend;
  spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
  suspend;
  for item in selector_0x42(arg0=0x01c4, arg1=0x0186, arg2=(10 * 32), origin=global[0x003c]) {
    post1 = item;
  }
  for item in selector_0x42(arg0=0x01c5, arg1=0x0184, arg2=(10 * 32), origin=global[0x003c]) {
    post2 = item;
  }
  if (post1 && post2) {
    PaletteFaderProcess.fadeToBlack(&global[0x003c]);
    AudioProcess.playSFXCru(0x00f2, &global[0x003c]);
    while (Container.destroyContents(0x00f2, &global[0x003c])) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }
    AudioProcess.playSFXCru(0x00f1, &global[0x003c]);
    if (dir) {
      Item.legal_create(0, post1);
      Item.legal_create(0, post2);
      counter = 1;
      while (counter <= 4) {
        Item.legal_create(counter, post1);
        Item.legal_create(counter, post2);
        Item.getQHi(0x0093, post1);
        spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
        suspend;
        counter = (1 + counter);
      }
    }
    else {
      counter = 0;
      while (counter <= 3) {
        Item.legal_create((7 - counter), post1);
        Item.legal_create((7 - counter), post2);
        Item.getQHi(0x0093, post1);
        spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
        suspend;
        counter = (1 + counter);
      }
    }
    spawn FREE.waitNTimerTicks(pid, 30, 0x00000000);
    suspend;
    Item.legal_create(0, post1);
    Item.legal_create(0, post2);
    Item.getQHi(0x0093, post1);
  }
  spawn FREE.waitNTimerTicks(pid, 120, 0x00000000);
  suspend;
  for item in selector_0x42(arg0=0x0193, arg1=0x01c7, arg2=(10 * 32), origin=global[0x003c]) {
    door = item;
    spawn DOOR.slot_21(pid, 1, door);
    suspend;
  }
  suspend;
  suspend;
  suspend;
  Actor.getHp(&global[0x003c]);
  return;
}
