function steam_slot_20() /* entry=2583 class_id=0x0a15 slot=0x20 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    item, /* [BP-02h] type=0x24 */
    detect, /* [BP-03h] type=0x62 */
    n; /* [BP-05h] type=0x24 */

  entry:
    set_info(0x0001, *(arg_06));

  while (true) {
    suspend;
    spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
    suspend;
    detect = 0;
    for item in nearby_items(family=6, origin=arg_06) {
      if (Actor.isNPC(item)) {
        if (Item.isOn(*(arg_06), item)) {
          n = item;
          if (n != getTargetNPCNumMaybe()) {
            NPC.slot_2b(n);
            if (!retval) {
              Actor.getSomeFlagProbablyCrouch(8, UCMachine.rndRange(Actor.setActivity(n), 16, 0), 0, n);
            }
          }
          else {
            Actor.getSomeFlagProbablyCrouch(UCMachine.rndRange(UCMachine.rndRange(8, 20, 10), 16, 0), 0, n);
          }
          detect = 1;
        }
      }
    }
    if (!detect) {
      ITEM.slot_24(4, arg_06);
      return;
    }
  }

  return;

}
