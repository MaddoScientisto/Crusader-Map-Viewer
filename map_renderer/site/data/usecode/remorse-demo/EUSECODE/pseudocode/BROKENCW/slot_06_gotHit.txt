function brokencw_gotHit() /* entry=211 class_id=0x00d1 slot=0x06 */
{
  set_info(0x020c, *(arg_06));
  process_exclude();
  /* loopscr */
  /* loop */
  while (condition) {
    if (intrinsic_0016(local_02) == 0x00d1) {
    }
    else if ((intrinsic_003f(local_02) == 6) && (Actor.isNPC(local_02) == 0)) {
      spawn ITEM.slot_28(pid, 1, 0, local_02);
      suspend;
    }
    /* loopnext */
  }
  return;
  /* decompilation stopped at SEARCH_SURFACE */
}
