function miss9_slot_20() /* entry=2327 class_id=0x0915 slot=0x20 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    target, /* [BP-02h] type=0x24 */
    station, /* [BP-04h] type=0x24 */
    screen, /* [BP-06h] type=0x24 */
    item, /* [BP-08h] type=0x24 */
    counter; /* [BP-10h] type=0x69 */

  entry:
    set_info(0x0001, *(arg_06));
    /* pop_global */

  block_019a:
    while (!intrinsic_0036()) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }

  block_01d0:
    /* global_address */
    /* global_address */
    suspend;

  block_01d0_selector_00:
    /* loop_selector item in nearby_items(shape=0x04c6, origin=arg_06) */

  block_021e:
    if condition goto block_022c;

  block_0221:
    station = item;
    /* loopnext */
    goto block_021e;

  block_022c_selector_00:
    /* loop_selector item in nearby_items(shape=0x037a, origin=arg_06) */

  block_024c:
    if condition goto block_026f;

  block_024f:
    if (intrinsic_00d9(item) != 99) goto block_026b;

  block_0264:
    screen = item;

  block_026b:
    /* loopnext */
    goto block_024c;

  block_026f_selector_00:
    for item in nearby_items(shape=0x053a, origin=arg_06) {
      target = item;
      /* loopnext */
    }

  block_029d:
    if target goto block_05b3;

  block_02a7_selector_00:
    /* loop_selector item in nearby_items(shape=15, origin=arg_06) */

  block_02c4:
    if condition goto block_02e5;

  block_02c7:
    if intrinsic_00d9(item) goto block_02e1;

  block_02e1:
    /* loopnext */
    goto block_02c4;

  block_02e7:
    while ((retval > 2) && (counter == 6)) {
      /* global_address */
      /* global_address */
      /* global_address */
      spawn NPC.slot_22(intrinsic_0037(intrinsic_0013(pid, 1, Item.legal_create(Item.legal_create(arg_06), intrinsic_0013(arg_06)))));
      suspend;
      counter = (1 + counter);
    }

  block_036a:
    /* global_address */
    /* global_address */
    suspend;
    spawn FREE.waitNTimerTicks(pid, 120, 0x00000000);
    suspend;
    spawn FREE.slot_3d(pid, Item.legal_create(target), intrinsic_0013(target), 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 100, 0x00000000);
    suspend;
    /* str_to_ptr */
    /* free_stack_string */
    suspend;
    spawn FREE.waitNTimerTicks(pid, 100, 0x00000000);
    suspend;
    spawn FREE.slot_3e(pid, 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 120, 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 100, 0x00000000);
    suspend;

  block_036a_selector_00:
    for item in nearby_items(shape=TELEPORTER_LIGHTS, origin=arg_06) {
      /* loopnext */
    }

  block_057a:
    spawn FREE.slot_26(pid, "9f", 0x00000000);
    /* free_stack_string */
    suspend;

  block_05b3:
    return;

}
