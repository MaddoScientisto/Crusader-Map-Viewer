function free_slot_26() /* entry=2574 class_id=0x0a0c slot=0x26 */
{
  var
    flick, /* [BP+06h] type=0x73 */
    localFlick; /* [BP-02h] type=0x73 */

  /* param_pid_chg */
  set_info(1, global[0x003c]);
  /* free_local_string */
  localFlick = flick;
  spawn FREE.waitNTimerTicks(pid, 60, 0x00000000);
  suspend;
  FREE.slot_41();
  while (Item.getFamilyOfType(&global[0x003c])) {
    spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
    suspend;
  }
  if (!Item.getItemFamily(&global[0x003c])) {
    while (Actor.doAnim(&global[0x003c])) {
      spawn FREE.waitNTimerTicks(pid, 2, 0x00000000);
      suspend;
    }
    suspend;
    suspend;
  }
  /* free_local_string */
  return;
}
