function scanner_use() /* entry=1327 class_id=0x052d slot=0x01 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    n; /* [BP-02h] type=0x24 */

  set_info(0x0207, *(arg_06));
  process_exclude();
  set_info(0x00f0, 0);
  if (Actor.setMana(&global[0x003c]) < 0x03e8) {
    spawn FREE.slot_21(pid, 0x00b0, 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 100, 0x00000000);
    suspend;
    return;
  }
  else {
    for n in nearby_items(family=6, origin=global[0x003c]) {
      if ((Item.getItemFamily(n) != 0) && (n == getTargetNPCNumMaybe())) {
        NPC.slot_2b(n);
        if (retval) {
          spawn SCANNER.slot_20(n, arg_06);
        }
      }
    }
    spawn FREE.slot_21(pid, 0x00ba, 0x00000000);
    suspend;
    spawn FREE.waitNTimerTicks(pid, 120, 0x00000000);
    suspend;
  }
  return;
}
