function melf_use() /* entry=957 class_id=0x03bb slot=0x01 */
{
  var
    referent, /* [BP+00h] type=0x69 */
    item; /* [BP-02h] type=0x24 */

  set_info(0x0207, *(arg_06));
  process_exclude();
  if (Actor.addHp(arg_06)) {
    return;
  }
  else {
    suspend;
    Actor.setDead(0x0096, 0x00c8, "mva5b", arg_06);
    suspend;
    playFlic(pid, 0x0096, 0x00c8, "5g", arg_06);
    suspend;
    spawn TRIGGER.slot_20(pid, 0, *(arg_06), arg_06);
    suspend;
    Item.pop(1, arg_06);
    while (condition) {
      if (Item.getQHi(item) == 34) {
        Item.setQLo(80, arg_06);
        AudioProcess.playSFXCru(61, arg_06);
        Item.destroy(item);
      }
    }
  }
  return;
}
